/**
 * Scaffold entry for the Next.js web application.
 *
 * The real App Router application (root layout, route groups, components) is
 * implemented in Part 4. This file exists so the workspace type-checks today and
 * proves that apps/web resolves workspace packages — the apps -> packages
 * dependency direction defined by ADR 0001. Part 4 replaces this entry with the
 * Next.js App Router structure from Notted.md.
 */

export { APP_NAME } from "@notted/shared-types";
