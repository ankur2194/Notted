/**
 * Scaffold entry for the NestJS API application.
 *
 * The real NestJS entry point (src/main.ts), root module (src/app.module.ts),
 * and module graph are implemented in Part 5. This file exists so the workspace
 * type-checks today and proves that apps/api resolves workspace packages — the
 * apps -> packages dependency direction defined by ADR 0001. Part 5 replaces
 * this entry with the NestJS structure from Notted.md.
 */

export { APP_NAME } from "@notted/shared-types";
