# Completed Parts

This directory is the durable implementation history for the numbered parts in the root [`Plan.md`](../../Plan.md). Its purpose is to let another developer, agent, or later session understand what has actually been built without reconstructing decisions from source code or conversation history.

## Required Workflow

1. Before starting a plan part, read the completion records for all prerequisite parts and inspect any open risks they identify.
2. Implement and verify the selected `Plan.md` part according to its stated requirements and completion criteria.
3. Copy `TEMPLATE.md` to `part-NN-short-name.md`, where `NN` is the zero-padded part number.
4. Replace every placeholder with factual information. Do not claim tests or commands that were not run.
5. If verification was skipped or failed, mark it clearly and do not describe the part as complete.
6. Add later corrections or discoveries to the existing record instead of creating a competing summary.

## Naming and Scope

- Use one primary record per numbered part: `part-01-architecture-decisions.md`, `part-02-monorepo-initialization.md`, and so on through `part-88-post-mvp-delivery.md`.
- Keep the record focused on the implemented part. Link related records rather than copying their content.
- Use repository-relative paths and include migration names, configuration keys, API routes, and important symbols where they help future work.
- Never include passwords, tokens, private keys, connection strings with credentials, personal data, or other secrets.
- A record may be prepared while work is ongoing, but its status must remain `In progress` until every required completion criterion passes.

## Status Values

- `Complete`: all stated completion criteria passed.
- `Complete with follow-up`: the part is usable and verified, but explicitly listed non-blocking work remains.
- `In progress`: implementation or required verification remains.
- `Blocked`: work cannot continue; the blocker and required resolution are recorded.
- `Superseded`: a later decision or implementation replaced this work; link the replacement record.

## Index

Add one row after creating each record. Keep rows ordered by part number.

| Part | Status   | Completed  | Summary                                                                                                                                                                 |
| ---: | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    1 | Complete | 2026-07-22 | Architecture boundaries, safe schema defaults, and a strictly resolved compatibility baseline.                                                                          |
|    2 | Complete | 2026-07-22 | pnpm + Turborepo monorepo scaffolding, four canonical workspaces, root task scripts, and strict runtime/package pins.                                                   |
|    3 | Complete | 2026-07-23 | App-scoped Next.js/NestJS + repo-wide TypeScript/a11y/import ESLint, Prettier, fail-on-warning scripts, and optional commit gates.                                      |
|    4 | Complete | 2026-07-27 | Next.js 16 App Router scaffold with canonical styles, minimal client islands, accessibility coverage, production build, and rendered HTTP smoke.                        |
|    5 | Complete | 2026-07-27 | NestJS API scaffold with typed configuration, safe HTTP bootstrap, health endpoints, structured logging, error envelopes, bounded rate limiting, and graceful shutdown. |
|    6 | Complete | 2026-07-24 | Verified framework-neutral shared types and strict Zod validators consumed through API and web package boundaries.                                                      |
|    8 | Complete | 2026-07-27 | Typed API/web/infrastructure environment contracts, safe examples, strict production preflight, dotenv-aware cross-file checks, and contract tests.                     |
|    9 | Complete | 2026-07-27 | Digest/source-pinned healthy development services, internal networking with host loopback access, non-root MinIO, private buckets, volumes, and runtime recovery proof. |
|   10 | Complete | 2026-07-27 | Canonical developer commands, local-daemon guarded reset, legacy-volume recovery guidance, and stepwise onboarding verification.                                        |
|   11 | Complete | 2026-07-27 | Narrow PostgreSQL, Redis, MinIO, Meilisearch, and SMTP clients with bounded lifecycle, coalesced readiness, and live loss/recovery proof.                               |
|   12 | Complete | 2026-07-27 | Drizzle/PostgreSQL providers, transaction helper, immutable extension migration, schema conventions, policy, and empty/pre-existing-data verification.                  |
|   13 | Complete | 2026-07-29 | Better Auth 1.6.24 identity schema with existing users model, database-generated UUID contract, boolean verification plus preserved timestamp, and auth-owned tables.   |
|   14 | Complete | 2026-07-29 | Workspace roots, memberships, invitations, roles, plans, quotas, uniqueness, and tenant-lifecycle foreign keys.                                                         |
|   15 | Complete | 2026-07-29 | Projects, folders, hierarchical notes, ordering, sharing/access grants, and composite cross-workspace foreign keys.                                                     |
|   16 | Complete | 2026-07-29 | Tags, attachment metadata, threaded comments, and uniquely keyed note-version snapshots.                                                                                |
|   17 | Complete | 2026-07-29 | Standalone tasks, custom statuses, nesting, recurrence, assignees, ordering, and task-tag links.                                                                        |
|   18 | Complete | 2026-07-29 | Operations/integration schema including durable job outbox intent and independent worker idempotency.                                                                   |
|   19 | Complete | 2026-07-29 | Repository-layer tenant context, strict pre-SQL guards, all-operation isolation matrices, and retention policy.                                                         |
|   20 | Complete | 2026-07-29 | Deterministic multi-tenant seed fixtures with idempotent writes and production/target-name safety refusal.                                                              |
|   21 | Complete | 2026-07-31 | Better Auth backend, opaque sessions, Redis acceleration, and encrypted durable auth-email delivery verified with live infrastructure. |
|   22 | Complete | 2026-07-31 | Accessible auth screens, safe redirects, server route protection, logout, and critical Chromium journeys verified. |
|   23 | Complete | 2026-07-31 | Optional OAuth, TOTP/recovery, passkeys, remember-me, recent authentication, and safe session controls verified. |
|   24 | Complete | 2026-07-31 | Central authorization policy, tenant-scoped facts, shared adapters, and cross-tenant denial verified. |
|   25 | Complete | 2026-07-31 | Responsive shell, server-validated workspace selection, persistent notifications, migration, accessibility, and browser journeys verified. |
|   26 | Complete | 2026-08-01 | Verified REST/tRPC workspace lifecycle with replay-safe creation, atomic owner membership, tenant authorization, durable deletion audit, and cleanup intent. |
|   27 | Complete | 2026-08-01 | Accessible workspace list/create, overview, settings, switching, permission states, and real-stack Chromium lifecycle/isolation journeys. |
|   28 | Complete | 2026-08-01 | Verified membership and single-use invitation lifecycle with role/last-owner safety, durable queued email delivery, acceptance UI, and audit evidence. |
|   29 | Complete | 2026-08-01 | Tenant-scoped project CRUD, filtering, lifecycle transitions, cover authorization, replay safety, durable domain events, and note/task preservation. |
|   30 | Complete | 2026-08-02 | Project list/detail UI, read projection, durable restriction state; Review #2: a11y, type-check, lint, integration all pass. |
|   31 | Complete | 2026-08-02 | Transactional note/folder APIs, deletion batches, safe cascade, versioned renormalization, index/concurrency artifacts; Review #2: calculatePosition fix, planner assertions, all gates pass. |
|   32 | Complete | 2026-08-02 | Note hierarchy/sharing UI, DnD, folders/trash, authenticated sharing, rollback injection; Review #2: a11y, clipboard race, keyboard DnD, focus mgmt, all gates pass. |
|   33 | Complete | 2026-08-03 | Versioned TipTap allow-list, bounded migration, safe URL/plain/HTML helpers, exact headless extensions, and verified ProseMirror round-trips. |
|   34 | Complete | 2026-08-03 | TipTap editor and roving-tabindex toolbar, drift-proof shortcut table and help dialog, sanitized link/colour dialogs, remount restoration; Review #2: ARIA and duplicate-render fixes, all gates pass. |
|   35 | Complete | 2026-08-03 | Table contract widening with bounds, resizable tables, restored nested checklists, lowlight code blocks, markdown rules, single Tab authority; Review #2: Table keymap strip fix, split-cell guard, all gates pass. |
|   36 | Complete | 2026-08-03 | Mention node contract, slash command menu, workspace-scoped mentions with removed-user fallback, shared suggestion popover; Review #2: member pagination and lazy directory fetch, all gates pass. |
|   37 | Complete | 2026-08-06 | A4/US Letter paper in physical CSS units, configurable margins, seven zoom settings with scaled scroll extents, server-persisted per-note page size. Measured in Chromium: 794x1123 and 816x1056 at 100%, size survives reload, controls unclipped at 390/768/1440. |
|   38 | Complete | 2026-08-06 | Additive `pageBreak` contract node, TipTap atom, `/page-break` command, two shortcuts, non-destructive overflow guides, focus mode, standalone `print.css` with a generated `@page` rule. A4/Letter PDF snapshots taken in Chromium — correct MediaBox, two pages from the explicit break, no chrome in print; browser pass also caught and fixed a break-deleted-by-typing defect. |
|   39 | Complete with follow-up | 2026-08-06 | Pure autosave state machine — one version cell, one in-flight request, coalesced content+settings PATCH, out-of-order discard, bounded backoff, halting conflict, in-memory offline queue, keepalive navigation flush, explicit retry/reload UI. All six named verify scenarios covered without a browser; Review #2 raised no blocking finding. |
|   40 | Complete | 2026-08-07 | Private-bucket image upload/download: `ObjectStorageService` byte plane, opaque `w/{ws}/a/{id}/{variant}/{token}{ext}` keys, busboy limits enforced before and during transfer, first-party magic-byte sniffing (AVIF rejected), filename sanitization, derived `FOR UPDATE` quota, pending/processing/ready/failed lifecycle with after-commit compensating cleanup, and proxied streaming with the mandatory `Cross-Origin-Resource-Policy: same-site` override. All gates executed and passing; live PostgreSQL + MinIO suites pass; **CORP confirmed in Chromium**, closing the phase's highest-risk item. Fixed the 304 `Content-Length` and mapped disabled storage to a clean 503; orphan sweep recorded as delegated to Part 45. Also made the API coverage gate runnable for the first time (writable container volumes for `coverage`/`test-results`, image-side `mkdir` for correct volume ownership, vitest `clean: false` + `reportOnFailure`) — in-container global 81.81/74.81/85.19/83.61 — and fixed a pre-existing `app.e2e.test.ts` failure that inherited the container's `BETTER_AUTH_TRUSTED_ORIGINS`. |
|   41 | Complete with follow-up | 2026-08-07 | Sharp-backed image pipeline filling Part 40's `IMAGE_PROCESSOR` seam: admission → header-only `.metadata()` → pixel/frame budget → pixels, so a decompression bomb dies before a decoder runs; `original` kept verbatim, `full` a metadata-stripped source-family re-encode bounded to 2000 px, `medium`/`thumbnail` WebP, and a 2 KiB inline WebP blur data URI that structurally cannot enter the document. Animation preserved in `full` only, SVG rasterized rather than sanitized (no servable `image/svg+xml`), HEIC converted behind a one-file seam with a byte cap and timeout, seven fully defaulted resource budgets, three new stable failure codes. `sharp 0.35.0` reconciled against `Notted.md`'s `0.33.x` in ADR 0008; All gates executed and passing (`src/attachments` coverage 94/87/95/96). Fixed a `sharp` `GifOptions` defect (`reoptimise` is not a key; `reuse: false` is the real option) that had failed `build` and left the API container restarting, corrected the EXIF fixture so the GPS-stripping assertion is no longer vacuous (libvips' GPS IFD is `IFD3`), and widened the SVG gates to namespace-prefixed elements. **Follow-up: `libheif-js` LGPL-3.0 still awaits human sign-off.** |
|   42 | Complete | 2026-08-07 | Browser half of image support: contract widened additively with a block `image` node whose four attributes contain **no `src`** — the absence is what makes "the saved document never relies on temporary blob/base64 URLs" structurally true, and a dedicated test proves every URL-shaped attribute is rejected. Temp upload state lives in ProseMirror **decorations**, never a pending node, so `getJSON()` is byte-identical and Part 39 autosave can never PATCH a dangling attachment reference. Hand-written `CustomImage.ts` (`@tiptap/extension-image` rejected: its data model is a `src`), XHR-over-fetch for upload progress with one idempotency key per file across retries, concurrency 3, blur-up node view with no layout shift, `/image` command and toolbar button that insert nothing until bytes have a permanent id. Schema version stays 1 with the bump trigger recorded. Four exact-set guardrail tests updated with intent, including hardening the editor's "no network I/O" rule to cover `XMLHttpRequest`. **Two real-browser defects found and fixed in the final pass.** A multi-file batch landed only one image: TipTap's `insertContentAt` widens a block insertion to swallow the enclosing paragraph, so the first completion's step spanned the position its siblings were anchored at and `DecorationSet.map` reported them deleted — broken for empty paragraphs, for a caret at the end of any paragraph, and for a whole-document replacement, while the one caret position every existing test used happened to survive. Fixed by re-anchoring deleted placeholders with `map(pos, 1)`, with a deliberate exception for whole-document replacement so an upload begun in one note cannot land in another. Separately, the decoration's `destroy` hook fired on every document change rather than on teardown, so one keystroke revoked every in-flight preview; revocation now belongs solely to the upload manager. All gates pass (web 1016; coverage 82.73/75.51/85.20/84.93); `note-images.spec.ts` passes 3/3 in Chromium including CORP `naturalWidth` and the 125% drop. **Carried forward: Part 43 must ship the alt-text editor.** |
|   43 | Complete | 2026-08-08 | Image manipulation UI. Contract widened additively with `align`/`wrap`/`fullWidth`/`caption` on the `image` node (schema version stays 1 — the accepted value domain is unchanged, so this is not a re-typing); `resolveNoteImageWrap` is the single place `fullWidth` + `wrap: "inline"` is resolved, and the renderer now emits a `<figure>`/`<figcaption>` projection. Plain-DOM node view gains four corner handles with Shift sampled live from both pointer moves and keydown/keyup, Escape cancel, and **one** `updateAttributes` per gesture so a drag is a single undo step; width is clamped by reading `--notted-page-content-width` off the paper rather than recomputing Part 37's arithmetic. Caption is a real `<input>` committed on Enter/blur/500 ms debounce and mirrored into a `<span>` because a field's value does not print. Alt-text dialog closes Part 42's carried-forward gap. Reduced motion swaps to Part 41's static `medium` poster. **Review round 2 found a genuine WCAG 2.2 AA failure invisible to 1162 passing unit tests:** `useRovingToolbar` bound its keydown listener from an effect whose only dependency was a `useCallback([])`, so for a toolbar that mounts hidden — `ImageToolbar` renders `null` until an image is selected — the single effect pass saw a null ref and the listener was never attached; the image toolbar's arrow keys were dead in the browser while `EditorToolbar` escaped it purely by always being mounted. The hook now returns an `onKeyDown` and the effect is gone, fixing both toolbars and deleting code, with a regression test verified to fail when the handler is unbound. Two e2e journeys were also mis-authored: a 64 px fixture asserting a >50 px shrink that `IMAGE_MIN_WIDTH_PX = 48` correctly makes impossible, and an undo assertion placed after `page.reload()` where the history stack is empty. All gates pass; 8/8 Chromium journeys green. |
|   44 | Complete | 2026-08-08 | Generic attachments end to end. `admitUpload()` is the single classifier — image signature, then generic-file signature (PDF/ZIP/DOCX/XLSX/RAR/7Z/TAR/GZIP/RTF, OOXML disambiguated by `[Content_Types].xml`), then text/code by extension allow-list plus whole-buffer NUL scan and bounded UTF-8 validation, stored as `text/plain`, which is what makes an uploaded `.html` inert. Both service methods re-run it so the transport cannot force a pipeline, and the stored extension is forced to the canonical extension of the **sniffed** type so `invoice.pdf.exe` cannot survive. Generic files always get `Content-Disposition: attachment` plus a newly added `X-Content-Type-Options: nosniff`; disposition is chosen from `mediaType`, not a client-supplied MIME string. New `attachment` block node carries no URL-shaped attribute of any kind (contract test covers `src`/`url`/`href`/`downloadUrl`/`contentUrl`/`objectKey` including `blob:` and `data:`), and `mimeType` is validated by a bounded grammar rather than a closed enum precisely because a desync would make `safeParseNoteDocument` reject the document and stop autosave silently. Plain-DOM card node view with React dialogs reached by bubbling `CustomEvent`, so nothing React-shaped enters the editor subtree. Upload queue **shared** with a `kind` discriminator, not duplicated. PDF preview renders to `<canvas>` only via pinned `pdfjs-dist@5.6.205` — never an iframe/embed/object — with `isEvalSupported: false` and the worker bundled locally; the bare-specifier `new URL(…, import.meta.url)` was the part's flagged risk and Turbopack does rewrite it, verified from the emitted bundle. No migration: `media_type='file'` and `variants.preview` already existed. All gates pass; both Chromium journeys green on first execution. |
|   45 | Complete | 2026-08-08 | Storage quotas and cleanup, closing Phase 7. Usage stays **derived** — no `storage_used_bytes` column — with `StorageQuotaService` splitting a locked write path (`SELECT … FOR UPDATE` + `SUM`, which `AttachmentsService.reserveQuota` now delegates to rather than duplicating) from an unlocked read path, so a settings page refreshing a usage bar can never block an upload. Per-plan defaults (free 1 GiB / pro 10 GiB / enterprise 100 GiB) are invented here and flagged as a product decision; pro equals the old `MAX_WORKSPACE_STORAGE_BYTES` default so shipped behaviour is unchanged. `GET .../storage` and an owner/admin `POST .../storage/maintenance`, both through the existing policy layer, plus a tRPC query and a report-only CLI. Four idempotent dry-run-capable sweeps with every selection predicate a pure function, a bounded `ObjectStore.listObjects`, and reports carrying counts and UUIDs but never keys or filenames. **Review round 1 caught two data-loss paths.** `STORAGE_MAINTENANCE_DRY_RUN` defaulted to `false` in code while `.env.example` said `true`, so an operator setting only `STORAGE_MAINTENANCE_ENABLED=true` in a secrets manager got an irreversible cascade purge one interval later — destroying bytes now takes two explicit acts. And reconciliation-marked rows were hard-deleted by the very next pass, because both windows measure from `created_at` and a row can only be marked `storage_object_missing` once it is already older than the orphan window; the exemption uses `is distinct from` rather than `ne()`, since `<>` yields NULL for a NULL `processing_error` and would have silently exempted every `failed` row with no error code from the reaper. The subtree traversal is deliberately **unscoped**: `notes_parent_id_notes_id_fk` is a plain self-reference with `ON DELETE CASCADE` and no composite `(workspace_id, parent_id)` key, so scoping would prevent zero deletions while blinding `hasLiveDescendant` and stranding the object keys `readSubtreeAttachmentKeys` must collect. Document-unreferenced attachments are reported, never reclaimed — undo has no deadline. No migration. All gates pass; 998 API tests and both live integration suites green in-container. |
|   46 | Complete with follow-up | 2026-08-10 | Tags and templates, opening Phase 8. **No migration** — every requirement was already satisfied by the Part 16 schema, and `tags.updated_at`, `tags.created_by_id`, `color NOT NULL`, and a colour CHECK were each considered and rejected with reasons. New `apps/api/src/tags/` module with `tag.read|create|update|delete` actions and a `"tag"` resource kind; usage counts come from two correlated subqueries joined through `notes` (`is_deleted = false`) and `tasks` under `whereWorkspace`, reported as **separate `noteCount` and `taskCount`** because a confirmation saying "used on 12 notes" while silently detaching 30 tasks is a data-loss surprise. Editors may create and update tags but **not delete** them — deletion strips a tag from every note and task in the workspace, matching `editorAllowed` already having no `note.delete` branch. Templates need no new server concept: `NotesService.copy` serves both directions with `asTemplate`, authorizing `note.read` on the source then `note.create` on the destination before any SQL, and **the absence of a source-link column is itself the "no accidental live link" guarantee**. `tagId` was silently dropped by `parseNoteSearchParams`, `noteSearch`, and `listSearch`, so sidebar filtering was inert until all three were threaded. Extracted the shared `lib/api/{request-json,server-read}` client that Part 47 and everything after builds on. Review round 2 found the one real gap: **a copied note shares the source's attachment rows**, so permanently deleting the source breaks every note made from the template, and a template copied across projects renders broken images for readers who cannot see the original — text and tags copy correctly, the fix needs a `copyObject` the `ObjectStore` interface does not have, and it is recorded as an explicit limitation rather than half-built unreviewed. All gates pass; 13/13 Playwright on a cold disposable stack; 83.15%/75.91% in-container coverage.
|   47 | Complete with follow-up | 2026-08-10 | Standalone task CRUD and list view. **No migration**; instead the Part-06 task contracts — which contradicted the Part-17 database on `cancelled`/`canceled`, `dueAt`/`due_date`, `position`/`sort_order`, and a recurrence enum missing `none` and `custom` — were rewritten against the schema, with `sortOrder` retyped `z.number().finite()` because midpoint inserts produce fractions and `createTaskSchema.position` replaced by a `beforeTaskId` anchor. Seven-method service, authorization before any SQL, serializable mutations behind `pg_advisory_xact_lock` sibling groups; a stale anchor answers **409 `ORDER_CONFLICT`** rather than guessing, which is why no `tasks.version` column was needed. Recurrence spawns **synchronously inside the completing transaction** and advances past `max(dueDate, now)` so a five-months-late daily task does not spawn an already-overdue successor; monthly clamps Jan 31 to Feb 28/29 from the original anchor. **Timezone contract:** UTC storage, client composes local date + optional time into the instant (no time ⇒ 00:00 local), cron fields are UTC and say so, overdue is client-side — with the one-hour DST drift named as the ceiling. **Zero production authorization changes**; editors remain denied `task.delete` via the trailing `return false`. Task deletion is hard and cascades, so bulk reports the true subtree count and confirms before destroying. Per an explicit user decision the task list renders **below** the paper editor rather than replacing it, deviating from `Notted.md`'s "simplified editor". The browser suite exposed a cross-cutting bug: `AuthorizationDeniedError` raised *inside* a handler had no REST translation at all, so every service-level denial returned **500 instead of 404** — fixed in the shared `ApiExceptionFilter` for all modules, not just tasks. All gates pass; 13/13 Playwright; the remaining gap is that editor row controls gate on note-level `canEdit` rather than `createdById` plus role.
|   48 | Complete with follow-up | 2026-08-11 | Board, calendar and progress views. **All three views share exactly one `taskQueryKeys.list` cache entry**: the board and calendar issue no task request of their own and partition the page the list already holds, which is the whole mechanism behind the "all views reflect the same state after mutation" bullet — adding a filtered per-view fetch would break it. `task_statuses` finally got CRUD (`/api/v1/workspaces/:id/task-statuses`, REST only, **no tRPC subrouter** — a deliberate deviation), managed by owner/admin through the **existing** `settings.update` action, so this part adds no authorization action, resource kind, or policy row. Workspace-level name uniqueness is enforced inside a `serializable` transaction because the unique index cannot under NULL `project_id` distinctness; delete needs no reassignment (`custom_status_id` is `ON DELETE SET NULL`) and returns an `affected` count for the confirmation. A cross-column move is **one** `updateTask` — `{customStatusId}` alone for a custom column, leaving built-in `status` and therefore `completed_at` untouched, honouring Part 47's no-`is_terminal` rule. Calendar grids are built with `new Date(y, m, d)` and **no `+86_400_000` anywhere**, which is what makes them DST-correct. **One migration, `0014`**, adding denormalized `notes.checklist_done`/`checklist_total` with a hand-appended recursive jsonb backfill mirroring the new `countChecklist`; all three writers go through one `contentProjection()` so the counters cannot drift from `content_plain`. Note and project progress now combine task rows and inline checklists through shared `sql-aggregates` helpers, changing `ProjectTaskProgress.coverage` to `"tasks-and-checklists"` and making `NoteSummary.progress` **required** — both breaking for out-of-tree clients. Part 47's editor-affordance defect is closed by projecting `createdById` onto the summary and threading a `viewer` prop, with no server-computed per-task capabilities. Review pass 1 found that deleting a board column **silently un-columned notes**, because Part 49 pointed `notes.board_column_id` at the same table while the delete path counted only tasks and a note has no fallback placement: the delete now returns a workspace-scoped `affectedNotes` and the dialog states the consequence in words before and after. **Review pass 2 re-ran every gate one at a time and all pass** — `format:check`, lint, `type-check`, test (7+307+1169+1302+17), API build, `db:check`, container coverage 83.44%/76.14% API and locally-run 79.86%/72.97% web — **except `pnpm build`, which fails in `env:validate` before `next build`** (the known environment failure Parts 45-47 record, so no production web bundle exists), and **e2e, which pass 2 did not run**; its 5/5 + 13/13 result is review pass 1's and predates the fix pass. The corrected `0014` predicate was re-proved without applying a migration — the CTE run as a pure `SELECT` agreed with the stored counters on 130/130 real rows and with `countChecklist` on 16/16 adversarial literals — but the file has still never run as a migration, so **already-migrated databases keep the pre-fix backfill permanently** and must be recreated to pick it up. Open follow-ups: the three `serializable` transactions still have **no bounded 40001 retry** (repo-wide gap); the calendar's windowed query is the one cache entry optimistic mutations do not write, self-healing only through a refetch on remount; and per-file coverage on the drag paths (`TaskStatusManager` 41%, `TaskBoard` 60%) sits below the floor the aggregates clear.
|   49 | Complete with follow-up | 2026-08-11 | Project board and timeline views, on a written-down view-switching contract. The note board **shares Part 48's `task_statuses` table** as its column vocabulary — `notes` gains one nullable `board_column_id` FK, and there is no second columns table, no second CRUD API and no second manager UI, so renaming a column renames it on both boards (user-confirmed, and the intent). Rather than a new endpoint duplicating `move`'s version check, cycle check, sibling locking and `positionFor`, `moveNoteSchema` gains `boardColumnId` as a **deliberately optional "omitted means keep"** field, breaking the schema's all-absolute symmetry because the three container fields are one coupled hierarchy decision while the column is an orthogonal axis — forcing every caller to echo a possibly-stale value would be a lost-update hazard. `resolveBoardColumn` sits strictly **outside** the `containerChanges` guard: the column is not inherited, so a column-only move bumps `version` by exactly 1 and touches no descendant, skipping the per-descendant re-authorization a container change triggers. Validation mirrors `assertCustomStatus` (workspace-scoped, `project_id IS NULL` or the **destination** project, foreign id **404 not 403**), and a cross-project move that would strand a project-scoped column **clears it to `null` rather than 409ing** — a hierarchy change must never fail over a board column. `SET NULL` on the FK drops orphaned notes into a leading "No column" bucket with no reassignment rule. The **timeline derives spans from existing dates only — no new start-date columns — so it is a chronological timeline, not a Gantt**, and says so in the file: project `createdAt`→`dueAt` (falling back to the max child end), note `createdAt`→`updatedAt`, task `createdAt`→`completedAt ?? dueDate`, with a missing/equal/**earlier-than-start** end clamped so `marker` falls out of the clamp and a negative width is unrepresentable. No end is ever invented and no record dropped; overlaps are ~12 lines of greedy lane packing in a pure `timeline.ts` with **no charting, date or virtualization dependency**, and "pagination for larger projects" is one 100-row window per axis plus native `content-visibility:auto`. Markup is **semantic list first, chart second** — an `<ol>` whose rows carry the whole meaning in words and whose bars are `aria-hidden` — so it degrades with CSS off and needs no ARIA gymnastics. The four-clause contract lives in `note-view.ts` and is compiler-enforced where it bites: one query key per scope (switching four views issues **no *note* request** — the timeline does read tasks under `taskQueryKeys.list`, which review pass 2 made the clause say out loud), one mutation shape (`moveNote` only), no view may add a `notes` column, and `NoteBrowser` stays sole owner of data, mutations, the single live region and the version-conflict banner. **Migration `0015`, additive and backfill-free** — `NULL` *is* the "No column" bucket. Two coordinator fixes during integration: the empty-notes state was short-circuiting before the view switch, hiding the board's columns and the timeline's tasks on a project with no notes; and the e2e asserted an undated task under "Not scheduled" when the derivation table makes it a **marker** — every record has a `createdAt`, so that bucket is unreachable with sound data and the test now proves the invariants that are real (nothing dropped, no end invented). Review pass 1 corrected this record's claim that a deleted column gives notes "the same fallback the task board has": it does not, and Part 48's delete path now reports the note count. **Review pass 2 re-ran every gate one at a time alongside Part 48 and all pass** — `format:check`, lint, `type-check`, test, API build, `db:check`, container and local coverage — **except `pnpm build`, which fails in `env:validate` before `next build`** (the known environment failure), so the three new client views remain unproven against a production RSC build, and **e2e, which pass 2 did not run**; its 5/5 + 13/13 Chromium result is review pass 1's. `0015` was applied to a throwaway database in pass 1 and never edited afterwards. Pass 2 also found `NoteTimeline`'s `active` prop dead — production always passed `true` — and it was removed. Open follow-up: `NoteBoard.tsx` per-file coverage is 47.6%/49.4%, below the floor the aggregates clear, and jsdom cannot exercise real pointer drag.
|   50 | Complete | 2026-08-12 | Four typed BullMQ execution queues plus a shared redacted DLQ; PostgreSQL-authoritative outbox and replay protection; reconciliation boundaries for ambiguous side effects; bounded retries/timeouts, graceful shutdown, export concurrency two, AI provider limits, queued email/maintenance work, readiness, and operator-only audited/redacted Bull Board retry. Two independent reviews and all gates passed, including fresh migrations and an isolated Redis restart/retry/DLQ probe. |
|   51 | Complete | 2026-08-12 | Versioned tenant-filtered note projection; idempotent index/delete consumers and transactionally emitted mutation intents across notes, tags, attachments, projects, and workspace purge; stable bounded tenant/all-workspace drift repair and guarded CLI. Two independent reviews and all gates passed, including isolated PostgreSQL + Meilisearch drift repair and real CLI execution. |
|   52 | Complete | 2026-08-13 | Authorized workspace full-text search, filters/sorting/highlights/pagination/suggestions, workspace-local recents, accessible palette/results UI, and live Meilisearch-backed Chromium verification. |
|   53 | Complete | 2026-08-13 | Provider-neutral fixed-dimension embeddings, deterministic source hashing/truncation, durable generation/reindexing, tenant-scoped semantic search, and safe unavailable fallback. |
|   54 | Complete | 2026-08-13 | Deterministic weighted lexical/vector fusion, authorized stable pagination, bounded candidate depth, text-only fallback, and non-sensitive tuning diagnostics. |
|   55 | Complete | 2026-08-14 | Verified accepted snapshots, collaborative cadence seam, and bounded plan-aware retention; live PostgreSQL and full repository coverage pass. |
|   56 | Complete | 2026-08-14 | Verified authorized history/diff/N+1 restore, search convergence, accessible UI, tenant isolation, and focused real-stack Chromium journey. |
|   57 | Complete | 2026-08-14 | Verified authenticated WebSocket-only multi-instance Socket.io infrastructure, Redis fan-out, private rooms, limits, revocation, readiness, and shutdown. |
|   58 | Complete | 2026-08-15 | Yjs as the in-session document authority over the Part 57 transport, closing the silent last-write-wins clobber two editors used to get. **The whole part rests on one column**: `note_collaboration_states.projected_note_version` records the `notes.version` the Yjs state corresponds to, and every projection writes `UPDATE notes … WHERE id = $1 AND version = $projected_note_version` — the same optimistic lock autosave uses — so the two writers contend for one column and exactly one wins; zero rows means a non-collaborative writer got there first, and the epoch bumps and rebuilds from `notes.content`. The **server holds no long-lived `Y.Doc`** (one is materialised per handshake and per projection, then discarded; live updates are validated, persisted and relayed, never applied server-side), so two API instances cannot diverge. Updates are **persisted before they are acknowledged**, and the handshake **joins the room before it reads state** — reading first drops any update committed during the read, permanently, because the socket is not in the room yet and Yjs never learns there is a gap. Migration `0019` adds two tables with no `workspace_id`, following the `note_versions` precedent. `y-prosemirror` was evaluated and **rejected for the API** (`prosemirrorJSONToYDoc` needs a ProseMirror `Schema` the API does not have, and the package drags `prosemirror-view` into Nest); first-party converters produce JSON that passes `safeParseNoteDocument` before anything persists. Projection is a 2 s / 30 s in-process debounce, **not a BullMQ job**. `restoreVersion` now resets the collaborative state inside its existing serializable transaction, discharging the obligation Part 56 recorded. Review pass 1 found collaborative editing **never engaged in a real browser** — four separate causes, the decisive one being that a note's first sync announces epoch 1 to the room, so the client received a reset for the very epoch its own handshake was about to deliver, tore down its `Y.Doc` mid-flight and latched at `connecting` forever. Review pass 2 then found that in-window remote frames were **dropped on the client**, giving back the loss the join-before-sync ordering exists to prevent; they are now buffered and drained against the acked epoch. The editor's remount key is the provider's **generation, not its epoch** — a `stale` ack replaces the document without moving the epoch, and an epoch key left TipTap writing into a destroyed `Y.Doc` for the whole re-handshake. All gates pass, including `test:ci` 6/6 (API 82.05/73.62/83.39/84.41), the gated realtime integration 4/4, and `collaboration.spec.ts` 5/5 twice. Ceilings: a ≤2 s degraded-solo clobber window, a ≤25 s authorization memo window, no offline durability across a page reload, and a debounced projection dropped on shutdown (flushing was tried and reverted — Nest closes the pool in `onModuleDestroy`, before `onApplicationShutdown`). |
|   59 | Complete | 2026-08-15 | Presence and collaboration UI, **zero new npm packages**. The roster is **derived, never stored** — `server.in(room).fetchSockets()` through the Redis adapter already in place — so there is no key to expire, no table, no sweep job, and no row that can outlive a process restart; that is ADR 0004's "presence expires and is never restored as business data" satisfied structurally rather than by a timer. **Awareness carries no identity**: the client sends a cursor, and `userId`, colour index and client id are server-issued on an authenticated announce. The gateway **refuses to relay an awareness frame whose encoded Yjs client identifier is not bound to the sending socket** — which is why the relay is a handler that sees the raw frame rather than an opaque broadcast, and why review pass 1's fix had to refuse when *no* row is bound, not merely when a bound one mismatches (the first version was bypassable by never announcing). Colour is `sha256(userId)[0] % 8`, so a person keeps it across reloads *and* notes, with the palette as eight CSS custom properties so contrast is auditable in one file. Stale carets are cleared by four independent layers, and `RealtimeGateway#cleanup()` — the single teardown point — must capture the room set **before** its own `state.rooms.clear()`. The viewer list is `aria-live="off"` on purpose: announcing every join and leave in a ten-person session is unusable. **Typing indicator deliberately skipped** — with character-level sync and a live coloured caret it restates what the caret already shows. Residual, documented at the binding site: display names are resolved client-side from the cached member directory, so a member can present another member's name; the identifier and colour cannot be forged. |
|   60 | Complete | 2026-08-15 | Inline comments, mentions and notifications — **zero new packages, zero migrations**. The four `anchor_*` columns already existed, so anchors are Yjs relative positions in `anchor_metadata` with `anchor_key = "yrel:1"` as a scheme discriminator; a deleted range makes `relativePositionToAbsolutePosition` return `null`, which makes the **orphaned state free** — the comment moves to an "Orphaned" section with its stored quote, never deleted and never re-guessed. Highlights are **ProseMirror decorations, not a document mark**: a mark would bump the document schema version and migrate stored documents, and `handleUpdate` runs `safeParseNoteDocument` on every transaction, so an unknown mark halts autosave outright. **The server never remaps** — it stores what the client sends. **Nothing was added to the authorization layer**: the actions, the three policy rows and the workspace-scoped comment fact loader all already existed; the list route authorizes `note.read` rather than `comment.read` because the latter is declared over a `comment` resource and cannot authorize a listing. Mention dedup is **true by construction** — `added = mentions(next) \ mentions(previous)`, so a re-save returns before touching the database — with two pre-existing unique indexes behind it and **no new partial index on `notifications`**. Implementation found a gap the design missed: **the Yjs projection writes `notes.content` directly and never passes through `NotesService.update`**, so a mention typed in a live collaborative session would have notified nobody; the producer is now on both writers. Review pass 1 found the worker **leaking restricted-project note titles** — a workspace member can be mentioned in a note whose project they cannot read — now closed with `authorizeUserJob("note.read")`, its regression test using a genuine viewer because owners and admins read every note. Resolution audit is the existing `is_resolved`/`resolved_at`/`resolved_by_id` columns surfaced and logged; **no `audit_logs` table was invented** (Part 71 owns it) and no unconsumed outbox row was emitted "for Part 71". `realtime:comment:changed` is server-to-client only, identifiers with no content — a relay would let an author push arbitrary text into a room and bypass `comment.read` for anyone who lost permission since. Live-PostgreSQL integration proves 404-not-403 cross-tenant, viewer-can-comment-but-not-resolve, non-creator denial, exactly-one notification across a double save, and zero rows for a foreign-workspace mention. Ceilings: `anchor_from`/`anchor_to` are not written back (print/export/SSR see creation-time offsets), a mention never re-notifies for the same triple, and the bell is request-driven with one visibility-change of staleness. |
|   61 | Complete | 2026-08-18 | One email subsystem, replacing inline string-concatenated HTML in two workers. **React Email through `@react-email/render` only** (`@react-email/components` is deprecated); `apps/api/tsconfig.json` gained `jsx: "react-jsx"` and the API ESLint rulesets widened to `{ts,tsx}`. `EmailRendererService.render(templateKey, props) => {subject, html, text}` is **deliberately the exact shape the old `renderAuthEmail()` returned** — that identity is the whole migration seam, and because the key union carries the five `AuthEmailPurpose` values verbatim, `email_deliveries.template_key` keeps its existing values and **no data migration was needed**. One generic `email.deliver` job on the existing `"transactional-email"` queue; the producer takes a **caller-supplied transaction** so an email intent commits atomically with the change that caused it, and the idempotency key derives from the *event* (`sha256({templateKey, recipient, relatedEntityType, relatedEntityId})`) so a replayed transaction collapses to one email. **SMTP failure maps to `reconciliation_required` + `PermanentQueueJobError`, never an auto-retry** — SMTP acceptance is ambiguous and retrying it is how a system double-sends. Mention email is produced **alongside** the notification intent, two intents and two failure domains, honouring Part 60's recorded boundary. Suppression is a **sentinel `email_deliveries` row**, not a new table. The export-ready email **never embeds a signed URL** (ADR 0005). **No new environment variables.** Review found the mention email promising an unsubscribe control that did not exist: the backend shipped a `POST` with no `GET`, so a toggle could not render its own state — closed with `getEmailPreference`, a `GET` route and `MentionEmailPreference.tsx`, rendered for **every member** rather than gated on `canManage`, because a member who cannot rename the workspace must still be able to stop being emailed. Also found suppression read case-insensitively but **deleted case-sensitively**, which would have stranded a legacy mixed-case recipient unsubscribed with no way back; both paths now share one predicate. Digest email deliberately deferred. Ceiling: `lower(recipient)` cannot use the btree index, and the emails were never inspected visually. |
|   62 | Complete | 2026-08-18 | The export job lifecycle, on scaffolding that already existed (table, queue lane at concurrency 2, policy actions, retention sweep) with nothing creating, running or serving an export. **Every transition is a conditional `UPDATE` carrying the expected prior status in the `WHERE`**; zero rows means someone else won the race — handled, never thrown. `ready -> expired` stays owned by `storage-maintenance.service.ts` and is not duplicated. **Download streams; there is no presigned URL** — MinIO's port is unpublished so a browser could not reach one anyway, ADR 0005 permits authorized streaming, and streaming leaves no bearer artifact to leak into logs, caches or a `Referer`; the response is deliberately stricter than the attachment route. `signed_url_expires_at` (download grant) and `object_expires_at` (retention) are **not collapsed** — that split is why the schema has two columns. **REST only, no tRPC subrouter**, since `apps/web` has no tRPC client. `NoteCapabilities` gained `canExport` from the existing policy; **no new authorization action, resource kind or policy row**. Sources are `["note"]` only by design. Part 62 shipped the `txt` renderer alone so the state machine was end-to-end verifiable before Part 63. Review caught three real defects: `rowScope`/`list` ignored the caller's declared `workspaceId` and scoped only off tenant context; `announce()` ran notification and email in one `try`, so a notification failure silently suppressed the email; and **a cancel that won the race against `markReady` leaked its bytes forever** — `markReady` is the only writer of `object_key`, and the Part 45 sweep selects on `isNotNull(objectKey)`, so no sweep could ever see the object and the code comment crediting it was wrong. The worker now deletes it. Deviation: the Plan says "short-lived signed download links"; this streams. Ceiling: a `processing` row can wedge permanently — anything throwing after a successful claim leaves it there, the retry's claim misses and the handler returns normally instead of dead-lettering; the upgrade path is a staleness reaper in the Part 45 sweep. |
|   63 | Complete | 2026-08-18 | Server-side PDF and HTML export paginating exactly as the editor prints. **The central problem was the app-to-app boundary**: the renderer runs in `apps/api`, but `print.css` and `page-geometry.ts` lived in `apps/web`, which ADR 0001 forbids importing and which a production API image will not contain. Three `git mv`s moved them into the shared packages, and **[ADR 0012](../decisions/0012-shared-package-charters.md)** records the resulting widening of both charters. The riskiest unknown — whether Next 16 resolves a bare package specifier in a CSS `@import` — was verified against the real PostCSS pipeline before anything else was built and later through a real `pnpm build`: the production chunk carries one `@page`, two `@media print` and **zero unresolved `@import`**. **CSS order is load-bearing and `pageRuleCss` must come LAST**; the original brief for this part specified the opposite, and the implementation caught it — `print.css` ships its own `@page`, `@page` has no selector, so the earlier order would have made **every US Letter export silently print A4** and ignored custom margins. The test asserts real ordering, not presence. **The same HTML string is the PDF's input**, so the two cannot drift. Geometry comes from `pageRuleCss` and **never** from Puppeteer's `format`/`margin`; margins arrive from client `localStorage` and are clamped server-side three times. **SSRF containment in four layers** — `setContent` only, JavaScript disabled, non-`data:` requests aborted with the count asserted, and `--host-resolver-rules=MAP * ~NOTFOUND` plus offline mode. Review found **two of those four layers had no assertion at all**, so deleting the resolver blackhole broke no test on a machine without Chromium; the launch arguments are now asserted directly. `--no-sandbox` is required (`USER node`), compensating controls recorded, real seccomp flagged for Part 79. Only `pdf` degrades when Chromium is absent. Docker: every existing consumer was made to **name its build target explicitly first**, or the appended `workspace-chromium` stage would have silently become the default for every service — measured 1.45 GB against 493 MB lean, 2 services fat and 6 lean. ADR 0008's claim that `puppeteer-core` is ESM-only was **wrong** and was corrected by runtime probe. Ceilings: the real-Chromium suite is 5/5 skipped for want of the binary, so the only end-to-end SSRF assertion and the only Letter-geometry check are unproven; `chromium` is installed unpinned; and no production API Dockerfile exists yet. |
|   64 | Complete | 2026-08-18 | The remaining four formats and the export UI. **Markdown is a hand-written walker over the typed `NoteDocument`, no library** — Turndown would mean HTML to parse to Markdown, needing a DOM in the API and discarding exactly the typed structure that lets task lists, page breaks, code-block language and mention identity survive. TXT reuses `extractNoteContentPlain` verbatim so search extraction and text export cannot disagree. DOCX via `docx@9.7.1`, ZIP via `fflate@0.8.3` (MIT, zero dependencies, synchronous buffer-in/buffer-out matching `putObject`). Every node and mark has an explicit mapping with documented fallbacks; a page break is an HTML comment in Markdown (`---` would parse as a setext heading) but a real page break in DOCX, and mention identity survives both. ZIP is bounded on entries, total bytes, per-attachment bytes, versions and comments, checks `signal.aborted` between entries, and **skips an oversized attachment rather than failing**, recording it in a `manifest.json` that makes the include/exclude matrix checkable in one assertion. The format switch stayed one `switch` in one service. UI: a dialog on the existing header action column, and the tree's **first `refetchInterval`** — stopping on every terminal status, 1s/2s/5s backoff, disabled while closed, hard-stopping at the queue timeout — behind a **root `exportQueryKeys`** deliberately not nested under `noteQueryKeys`, because an export outlives its note and nesting would let a note invalidation kill a running poll. Review found **`EXPORT_MAX_ARTIFACT_BYTES` documented as a hard cap but enforced on two formats of six**, so four formats could exceed it and still be uploaded; the cap now applies once for every format, with the two inner checks kept because they stop work early rather than discarding it. Also found `docx` missing the node-type tripwire `markdown` has, so a new contract node would have exported as nothing at all. Deviations: `pdf_renderer_unavailable` does not exist (Chromium-missing surfaces as `generation_failed`); `export-filenames.ts` was unnecessary; the worker needed six lines the plan said it would not. **`jszip@3.10.1` `(MIT OR GPL-3.0-or-later)` is in the tree transitively under `docx`** — MIT elected and triaged, but stated plainly because `jszip` was rejected as a *direct* dependency on exactly that ground. Ceiling: **Playwright has never been executed for this part** — an end-to-end attempt exhausted the WSL2 host and froze the machine, and the run was deferred rather than retried; the resulting resource rules are now in `CLAUDE.md`, `AGENTS.md`, `docs/README.md` and `docs/standards/testing.md`. DOCX/Markdown output has also never been opened in a real consumer application. |
|   65 | Complete | 2026-08-19 | The public REST API, built by **reusing the 19 existing `/api/v1` controllers rather than adding a parallel surface** — zero controller and zero service changes. A valid key installs a **synthetic `AuthenticatedPrincipal` for the key's `created_by_id`** plus a separate `ApiKeyAuthorizationActor`, so effective permission is **scope ∩ creator's live workspace role**, failing closed by itself when the creator is demoted or removed. **No migration** — `api_keys` is used exactly as Part 18 shipped it, and `apiKey.*` actions, the `apiKey` resource kind, and `decideApiKey`'s admin-scope requirement all already existed. Credential is `Bearer ntd_pk_<32 base64url>` (192 bits) hashed with **deterministic HMAC-SHA256 peppered by `BETTER_AUTH_SECRET`, chosen over argon2 because a per-row salted KDF cannot be a lookup key** — authentication would have to scan and verify every candidate row, whereas HMAC keeps `api_keys_key_hash_unique` a single probe; 192 bits of CSPRNG has nothing low-entropy to guess. Cost: **rotating `BETTER_AUTH_SECRET` invalidates every issued key**, documented. Unknown/revoked/expired collapse to **one identical 401** (no enumeration oracle) and the row is **never cached**, so revocation bites on the next request. Blast radius closed by a **default-deny `APP_GUARD`**: an API-key request reaching a handler with no `AUTHORIZATION_HTTP_SPEC` is refused, so routes added later are shut to keys until deliberately opened; tRPC is rejected by path. **Merge review found a cross-tenant escalation**: `authorizeMachine` derives tenant context from `actor.workspaceId` and never sees the route's workspace, so a key for workspace A on a workspace-B path was authorized against A and then operated on B via the creator's principal — any creator in **both** workspaces carried the key across the boundary. Fixed in `AuthorizationHttpGuard` with 404-never-403; the seeded tenants are disjoint so the e2e case would have passed regardless, and now says so. Also fixed the `/api/v1` pre-guard `catch` that **collapsed every error into 503**, which would have reported an invalid key as an outage. OpenAPI ships with **zero new dependencies** via `z.toJSONSchema`, discovering routes from Nest decorator metadata without booting the app (**93 found, 93 documented, both directions tested**), with an `override` collapsing the recursive TipTap schema. Three rate-limit tiers on already-disjoint buckets plus a `:sensitive` bucket on key creation alone. **Nothing was executed** — every gate is deferred to the review agent. **Review round 1** disproved the record's own "an `admin` key can mint successor keys" ceiling: `decideApiKey` does return before the policy's freshness gate, but every high-risk action is re-authorized inside its service through `authorizeUser` with the key's synthetic, never-fresh principal, which answers 403 `RECENT_AUTHENTICATION_REQUIRED` — so **no API key can mint or revoke a key, touch members, billing, settings, webhooks, or delete a workspace**, and a stolen key cannot outlive its revocation. Round 1 also fixed two real defects: `PUT .../shares/{userId}` declared the read-class `note.read` on a mutation, letting a `read`-scope key create a delegation grant (the class is now closed at source — `ApiKeyRouteGuard` refuses any unsafe HTTP method from a key holding neither `write` nor `admin`), and `AuthService.authenticate` discarded the pre-guard's synthetic principal whenever Better Auth was unconfigured, 401-ing every API-key request on a Redis-less deployment. **Re-review pending.** |
|   66 | Complete | 2026-08-19 | Outbound webhooks end to end, on a **dedicated `webhook.deliver` outbox intent** rather than a handler on the existing `note.*`/`project.*` job types: `QueueHandlerRegistry.register` allows one handler per job type (subscribing would permanently claim four shared types for one feature) and a shared job means **one BullMQ retry budget across every endpoint**, letting one dead receiver drag healthy ones through retries. One intent per (endpoint × event) gives each its own **5-attempt** budget, delivered by two *additive* queue-core changes (`OutboxJobDefinition.maximumAttempts`, `QueueJobContext.attempt`/`.maximumAttempts`) with `QUEUE_ATTEMPTS` untouched. Producers are **three one-line calls inside existing transactions**, and the event filter runs before any SQL so `note.moved`/`folder.*` cost zero round trips. Migration **0020** adds `webhook_deliveries.event_id` — stable across every retry AND across a manual replay, which is what makes `X-Notted-Event-Id` a deduplication key and lets a replay rebuild the authoritative body from `job_outbox`; **no FK**, because outbox rows are prunable. HTTP uses **`node:https.request` with a custom `lookup`, not `fetch`** — the only way to pin the resolved IP while keeping TLS SNI and certificate validation correct without importing `undici`, and it makes no-redirect behaviour free. **SSRF is six separately-tested layers**, including a pre-flight DNS check that rejects if *any* address is blocked (defeating split round-robin) and a **connect-time re-check** so a TTL-0 rebind cannot land on a private IP. The worker opens a finite system authority that deliberately cannot read a note, then **re-authorizes the endpoint's `created_by_id` against the live resource**, so a restricted-project note never reaches an endpoint whose creator lost access. A retryable failure on the final attempt records `failed` and *returns*: **nothing dead-letters for an ordinary bad receiver**. Deliberate divergence from Part 61's "ambiguous acceptance never auto-retries" — an HTTP delivery **is** safely retryable where an SMTP send is not, because the stable event id lets a receiver deduplicate. Secrets are AES-256-GCM with AAD binding the ciphertext to its row, returned by exactly two routes; the contract test now enforces an **allow-list** of secret-exposing routes. **Reconciliation found six real defects across the subagent slices**, all fixed: a `URL` global that broke `type-check` repo-wide, **62 broken call sites** from the required context fields, 6 false failures from `rejects` on a *synchronous* throw, a stale secret allow-list, a hand-rolled `DELETE` client built on a false 204 premise, and a wrong environment doc. Type-check is clean in all three packages and **269 focused tests pass**; `lint`/`build`/full `test` and the two DB-backed suites are **deferred to review and unrun**. **Review round 1** confirmed the flagged open question — the `HIGH_RISK_ACTIONS` freshness gate does make five routes unreachable by any API key, and Part 65's record was corrected rather than the gate weakened. Round 1 also made the sender's timeout deterministic (it latches the outcome before destroying the socket, so a silent receiver can no longer be recorded as `connection_failed` by its own teardown, and Happy Eyeballs is off so one send is one connection attempt), taught `guardedLookup` to honour the requested address family, added `192.88.99.0/24` and the v4-compatible `::a.b.c.d` forms to the SSRF filter, corrected the endpoint-cap and verify-audit comments to match behaviour, listed the verify endpoint as a bounded outbound oracle, and stopped a running development API container's outbox dispatcher from delivering the integration suite's own intent a second time. **Review round 2** verified every fix — `webhooks.integration` 10/10 with the dev API container running (the exact prior exposure) and the silent-receiver case stable across three runs — and its last two hardening notes were taken: non-compressed v4-compatible spellings (`::0:7f00:1`) are now denied via an explicit `::/96` check with canonical `::`/`::1` carve-outs, and a family miss reports `ENOTFOUND` instead of masquerading as an SSRF denial. Success-payload shape settled by **ADR 0013** (bare payloads; errors enveloped). Final gate green 2026-08-19: format/lint/type-check/test/build/test:ci pass, `webhooks.e2e` 11/11, migration 0020 verified applied, coverage thresholds met. |
|   67 | Complete | 2026-08-25 | Provider-neutral AI configuration and the fail-closed gate every AI request passes through, on schema Part 18 already shipped — **no migration**. **Zero AI SDKs** (ADR 0008): both adapters are hand-rolled `fetch` over a shared line-buffered SSE reader, following the Part 53 embedding precedent, and **provider errors are derived from the HTTP status and nothing else** — a provider's error body echoes the prompt back and names the account, so it is never read on the failure path, never logged, never propagated; a test feeds a marker string in the body and asserts it reaches neither the message nor `String(error)`. **Neither adapter ever sends `tools`** — note text is untrusted input reaching a model, and a model that cannot call a tool cannot be talked into calling one. `FEATURE_AI_ENABLED` is a **kill-switch, never a credential source**: the workspace row is the only place a key comes from, so one workspace can never spend the operator's credential. `AiCredentialService` is a deliberate near-copy of `WebhookSecretService` — AES-256-GCM, one packed base64 column, **AAD binding the blob to its config row and key version**, which forces the row id to be minted with `randomUUID()` *before* the insert rather than read back from `DEFAULT`; a blob lifted into another workspace's row fails to decrypt instead of quietly billing that workspace against a key its admin never supplied. Rotation is **lazy with no batch job** (a row migrates on the next admin save, the one moment nobody is mid-request against it); disabling **clears** the ciphertext rather than parking a secret with no owner and no expiry. `AiGovernanceService.acquire()` runs seven ordered checks and **every branch fails closed** — missing row, unparseable settings blob, `null` Redis, a `redis.eval` that throws, a quota query that throws — the opposite of the usual availability instinct, and deliberate, because the thing being rationed is a metered third-party API and an outage is exactly when a retry storm arrives. Consent is checked **twice**, and the gate's re-read is authoritative: the schema refinement guards a form, this guards the moment note content leaves our servers, and it is the only one that still holds against a hand-edited settings blob. The grant carries an **exactly-once `recordUsage`** (flag flipped before the await) that **never throws outward**, so a lost metering row cannot fail a request whose answer the user already has; quota and rate-limit refusals write their own `rate_limited` row so an admin sees a ceiling rather than AI mysteriously not working. `hasCredentials` is computed, never projected — the shared selection object has **no ciphertext key at all**, so a leak needs an addition rather than a forgotten removal. Two new actions: `ai.configure` (owner/admin, `HIGH_RISK`) and `ai.use` (editors yes, viewers never), plus a rule stricter than the `webhook.*` precedent — **no API key reaches any `ai.*` action at any scope**, since configuring writes key material and using spends money against a credential the key's holder never supplied. Integration review of the delegated slices caught three things: a provider-switch refusal so wide it blocked leaving `disabled` **without any old key to carry across** (an admin could not pick a provider before fetching its key), a `sql<boolean>` inside `.returning()` with no precedent in this repository and narrower accepted shapes than `.select()`, and a circular type in the new integration harness. `ApiErrorCode` gained six members rather than folding into `FORBIDDEN`/`CONFLICT`/`RATE_LIMITED`, because each has a different remedy and the client renders the remedy, not the status; the retry delay rides on `AiGovernanceRefusal.retryAfterMs` instead of widening `ApiError.details` for one number. An unknown model yields a **`null` cost, not a guess** — tokens are recorded either way. **Ceiling: nothing in this part has ever been executed.** Implementation only; `lint`/`format:check`/`type-check`/`test`/`build` are all deferred to the session reviewer, and every unit suite plus `ai.integration.test.ts` is authored but unrun. Only the two shared packages were compiled, and only because `docs/openapi.json` regeneration needs them (428 insertions, 0 deletions, four AI routes). Also skipped: test-connection endpoint, tRPC router, Playwright specs. Deviation: `apps/web/src/lib/ai/` is not named in `Notted.md`, which describes no frontend AI surface. **Review round 1** (gates first run) fixed a test-helper type error and import order; the 17-case integration suite then ran live for the first time (17/17: cross-workspace usage isolation, role denials, every governance refusal). **Review round 2** added `AI_REQUEST_TIMEOUT_MS` as an outbound deadline on every chat call (`timeout` provider error code), dropped the now-unneeded deployment-level provider-key requirement, and noted the check-then-act quota ceiling. Final gate green 2026-08-25. |
|   68 | Complete | 2026-08-25 | Summarize, continue writing, and tone rewrite over the Part 67 seam — **synchronous SSE, no queue**: a job cannot stream to a caller. The ordering is the design: authorize `note.read`, take the governance grant, resolve the provider, and **only then** turn the response into an event stream, so every refusal is still an ordinary JSON envelope — flush one SSE byte first and a refusal becomes a `200` with an error frame inside it, needing a second client error path and reporting success to every proxy between. `finally` writes **exactly one `ai_usage` row on all five paths**, including a client cancel (charged at an estimated 4 chars/token, `ponytail:` marked — not charging makes cancel a free way to spend a workspace's budget). Prompts are frozen and versioned (`summarize.v1`/`continue.v1`/`rewrite.v1`), note text is framed as untrusted data inside `<note_content>` delimiters, a smuggled closing tag is stripped case-insensitively, and there is **no `tools` field to abuse**. Input bounded at 24k/8k/4k chars, output at both a token and a server-enforced character ceiling. **Integration review found three blocking defects this part introduced, all fixed before commit:** (1) the feature **streamed nothing** — global `compression()` matched `text/event-stream` through its `/^text\//` fallback and a gzip stream only flushes on `end()`, so every delta was buffered to completion; fixed with `Cache-Control: no-transform` and pinned by an assertion. (2) **Every accept path could delete the author's live selection** — TipTap's `insertContent` *is* `insertContentAt({from: selection.from, to: selection.to})`, so the stale-rewrite fallback destroyed the very range the guard had just refused to touch; fixed by inserting at a collapsed position. (3) **Model output was HTML-parsed** on two accept paths, corrupting `if (x < y)` and opening a real injection sink — a note talking the model into `<img src=…>` would have produced a live node in someone else's document; fixed by building JSON text nodes, which is the actual control the prompt only asks for. Four should-fix findings also taken. `Mod-Enter` beats StarterKit's HardBreak by **extension order, not priority** (`ExtensionManager` reverses the array; the comparator ties at equal priority), making `EditorShortcuts`' last position load-bearing; returning `false` falls through to a hard break as before. Three continue triggers share a one-slot module store on the `focus-mode.ts` precedent. **No migration, no new dependency, no new env var.** Gates (`lint`/`format:check`/`type-check`/`test`/`build`) were **deliberately not run** in this implement-only session and are deferred to the session reviewer; e2e specs skipped, and the compression fix in particular needs one live request to confirm empirically. **Review round 1** caught the summary insert splitting the author's paragraph (block nodes at an inline position) — fixed with `blockInsertPos` (after the containing block, nothing deleted). **Review round 2** armed the provider deadline in `run()` and moved `blockInsertPos` to `lib/ai/insert-position.ts` so every block-writing feature shares it. Final gate green 2026-08-25; live SSE flush behind `compression()` remains statically verified only. |
|   69 | Complete | 2026-08-25 | Meeting extraction and tag suggestion — the first **structured** AI output in the product, and the first non-streaming one. Both endpoints are synchronous JSON and persist **nothing**: a transcript is read, extracted and forgotten, and a suggestion assigns no tag. Malformed output is handled by a shared `json-repair.ts` — strip fences, `JSON.parse`, `safeParse`, then **exactly one** repair pass re-prompting with the zod issue and the rejected reply, then `422 AI_OUTPUT_INVALID`. The helper makes **no provider call itself**, which is what makes "at most two calls" a property of the code; both calls are metered, so a repair is a second `ai_usage` row and a second rate-limit slot, not a freebie. The repair prompt re-quotes the rejected output inside its own stripped delimiters, because that output derives from the transcript and is hostile too. `AiStreamService.complete()` is the new non-streaming seam (Part 70 consumes it): same prologue and same `finally` as Part 68's `run()`, reusing its `outcome()` helper so metering is literally the same code rather than a parallel one. Guardrails were factored into a delimiter-parameterised helper so the JSON features carry a JSON-only output rule where the streaming ones carry the plain-text rule — `AI_PROMPT_GUARDRAILS` is reassembled **byte-identically** because its test asserts it verbatim. **The model is never shown a tag id**: it returns names, and the server partitions them against a `workspace_id`-pinned pool into `existing` (real ids, pool spelling) and `proposed`, so "only authorized existing tags" is enforced by the partition and not by the prompt; a same-named tag in another workspace comes back as `proposed` with its id nowhere in the response. The model-output schema is deliberately **not** `.strict()` (a correct answer with one extra key is not worth a second billed call; zod strips it) while every request schema still is, and every list is capped **in the contract** — a model asked for "the action items" can return four hundred, and a review screen nobody can finish reading is not a safeguard. Client side: `requestJson` gained `timeoutMs`/`signal` merged via `AbortSignal.any`, because the house 8 s default would report a working 100 000-character extraction as a network fault (extraction 120 s, tags 30 s). **Nothing is written before Insert/Apply** — the Plan's own criterion — and both review screens have a test asserting it; action items default to *not* creating a task and carry an "already exists" badge from a bounded task page. Inserted content is built as **ProseMirror JSON, never an HTML string** (Part 68's injection-sink lesson, applied identically), and `taskList`/`taskItem`/`checked` were checked against the document contract rather than assumed, since a rejected node halts autosave. `TagSuggestions` mounts beside the **editor**, not the note list's tag picker, because the suggestion source must be the document as the author currently sees it and Part 58 lets the stored row lag; Apply **re-reads the note immediately before writing** since Part 39 autosave bumps `version` continuously and any earlier-captured version loses the CAS. Adding a slash entry broke two registry-completeness suites by design; both were repaired, the execution suite by registering a handler in `beforeEach` since the entry's availability reads a one-slot store. **No migration, no new dependency, no new env var.** Gates (`lint`/`format:check`/`type-check`/`test`/`build`) were **deliberately not run** in this implement-only session and are deferred to the session reviewer; e2e skipped. Two typing choices are unverified and flagged for the reviewer: the zod generic in `json-repair.ts` and a union-typed handler call in the controller test. The two JSON routes carry no `Retry-After` header (the envelope's `retryAfterMs` is unaffected); on-save passive auto-tagging is deferred. **Review round 1** sanitised zod paths in the repair prompt. **Review round 2** found the same paragraph-split defect in `MeetingExtractionDialog` (block-only meeting nodes at `selection.to`), now routed through the shared `blockInsertPos`, and armed the provider deadline in `complete()`, which has no client-close hook. Final gate green 2026-08-25. |
|   70 | Complete | 2026-08-25 | Grammar and style assistance — the first AI feature that sends note text **without the author pressing anything per request**, which is what makes the gate and the disclosure load-bearing rather than decorative. The contract is **segment-shaped, not note-shaped**: ≤20 segments of ≤2 000 chars, each with an opaque client key, every offset relative to that segment's own string, so the server is never sent a document position and cannot return one. Segment `text` is the one text field in the schema file that is **deliberately not trimmed** — it is a coordinate system, not a value, and trimming would shift every returned offset one character left; `id` *does* trim, to stay symmetric with the model-echo schema (an asymmetry found in review, where a padded id would round-trip, come back trimmed, miss the lookup, and be dropped for a reason no reader would guess). Server-side, `grammar.v1` takes one `complete()` plus at most one repair, then re-checks every offset against the text actually sent and **silently drops** unknown segment ids, `!(0 <= start < end <= len)`, and identity replacements — silent because a suggestion is note content (ADR 0007 keeps it out of logs) and the browser re-validates everything anyway, so a drop costs nothing while failing the request would discard a useful answer over one bad offset. The segment **id is stripped and JSON-quoted** before reaching the prompt: it is the one caller-controlled string landing in *instruction* space rather than inside a delimiter, so a forged `</segment>` in a 64-char id would otherwise open a line of its own. Client-side the whole safety story is **re-proving positions against the live document twice** — once when the response lands (the block may have been edited in flight, here or by a collaborator) and again at Accept, since a suggestion sits on screen as long as the author leaves it there — with positions held as **Part 60 anchors** rather than numbers in between, reused verbatim rather than forked. Both draw gates matter: the anchor must resolve **and** `doc.textBetween(from,to)` must still equal the text the suggestion was computed from, so a stale suggestion is *invisible* rather than merely unappliable — putting that guard only in Accept would mean showing the author a correction for text they did not write. Underlines are **decorations, never marks** (a mark the contract does not know halts autosave, broadcasts through Yjs, and lands in every export), redrawn by a meta-only transaction carrying no steps. Blocks are hashed with FNV-1a and **only changed blocks are sent**, batched at 20 on a 1.5 s debounce that also fires for remote Yjs transactions; the hash *is* the segment id. Accept inserts a ProseMirror **text node, never a string** (Part 68's injection-sink lesson, on a path the model reaches far more often) or `deleteRange` for an empty replacement; dismiss is keyed by `hash(original+replacement)` so it names the *correction* and survives a re-check. **Disable is structural** — the scheduler refuses before arming a timer and aborts what is in flight, so "off" is the absence of a request path, not a check on the way back from one. Enablement is per-user `localStorage`, default off, with `setEnabled(true)` reachable **only** from the disclosure dialog's confirm button until the key exists. Colour tokens deviate from the brief because `--color-warning` is already the comment highlight and a shared colour would read as a comment; dotted `text-decoration` composes with the comment's `border-bottom` instead of overwriting it. **`Alt+ArrowDown` is the keyboard route** (a decoration is not focusable, and Enter/Space are how a writer types) but is named in the toggle's hint rather than registered in the frozen `EDITOR_SHORTCUTS`, which would have meant reopening the `EditorShortcuts`-stays-last ordering invariant for one chord. **No migration, no new dependency, no new env var, no new error code.** Gates (`lint`/`format:check`/`type-check`/`test`/`build`) were **deliberately not run** in this implement-only session and are deferred to the session reviewer; e2e skipped. Known ceilings: prose inside lists/quotes/tables is sent and then discarded because `offset + 1` is a *textblock*'s first text position and a wrapper's `textContent` crosses boundaries positions count — `ponytail:` marked with the `descendants`/`isTextblock` fix, and the most valuable follow-up here; blocks over 2 000 chars are skipped whole; the preference is per browser. **Review round 1** fixed a portal cleanup-order test defect and added a genuine remote-origin (`ySyncPluginKey`) transaction case proving stale suggestions still drop under collaboration. **Review round 2**: no code change; the shared provider deadline now bounds grammar checks too. Final gate green 2026-08-25. |
|   71 | Complete | 2026-08-25 | Audit logging and administrative views — the part that makes `audit_logs` binding rather than conventional. **The table already existed (Part 18); what was missing was a writer, immutability, and a reader.** Thirteen services each hand-rolled their own `insert(auditLogs)`, none of which ever set `ip_address` or `user_agent` — because `AuthorizationEntryService.run()` rebuilds the tenant context from `{workspaceId, userId}` alone and the Express `Request` never reaches the service that audits. The fix is a **request `AsyncLocalStorage`** entered by the existing middleware (one store covers REST, tRPC and Better Auth) plus **one pure `recordAudit(db, event)`** taking the caller's transaction, so the row still commits with the mutation (ADR 0006) — a pure function and not a provider, because a provider would have added a constructor argument to thirteen classes and a stub to every one of their suites for nothing. Immutability moved from a comment to the DATABASE: migration 0021's `BEFORE UPDATE OR DELETE` trigger raises 42501 with exactly two exemptions — a referential action, recognised by `pg_trigger_depth() > 1` rather than by inventing a flag, and a DELETE under the transaction-local `notted.audit_purge` that only the new retention sweep and three test fixtures set. **The authorization change is the one that would have been silently wrong:** `audit.read` had to be denied to editors and viewers *by prefix*, above the generic `.read` suffix rule that would otherwise have handed every viewer the workspace's trail — same trap in `decideApiKey`, where a read-scoped key would have qualified. Metadata redaction is exact-name plus suffix, never substring, verified against every key the thirteen writers already pass (`keyPrefix`, `encryptionKeyVersion`, `contentConsent`, `credentialChanged` would all have been destroyed). New events for comments and exports; `ExportService.cancel` became transactional so a double-click audits nothing. CSV export is capped at 10 000 rows, formula-injection guarded, and admin-only. **Auth events are deliberately NOT recorded** — no workspace, and `workspace_id` is `NOT NULL`. Retention sweep has no supporting index (named ceiling, Part 55's 0018 is the upgrade path). All quality gates green after two review rounds — see the record. |
|   72 | Complete | 2026-08-25 | Branding and customization — a workspace logo, an accent colour, and a deliberate refusal to ship custom CSS, on **two columns that already existed**: `workspaces.logo_url` (Part 18, never once written by first-party code) and `workspaces.settings.accentColor` (persisted and *seeded* since Part 26 while `normalizeStoredSettings` silently **stripped** it on the way out) — so this part is the first writer into either and there is **NO migration**, no backfill, no lock, no rollback SQL. The logo is a **dedicated route, not an `attachments` row**, because the consumer that decides the design is an `<img src>` in a mail client: no session, no `Origin`, no way to follow a login redirect — so `POST`/`DELETE .../logo` authorize `settings.update` and require a trusted mutation origin, while `GET .../logo/{token}` is **public and unguarded**, its authorization a 128-bit token that is both the object-key suffix and the path segment, `timingSafeEqual`-compared, rotated on every replace or delete so old URLs (including ones sitting in old email) stop resolving, with **every miss answering the same 404** so the route is not an existence oracle. Uploads are sniffed (declared type ignored, ADR 0005), capped at **2 MiB enforced during transfer**, and re-encoded to a 200 px WebP, so the bytes served are always ones this API produced — `inline` plus `nosniff` plus `public, max-age=31536000, immutable`, the token making a shared cache safe where the attachment route needs `private`. `logoUrl` is stored **app-relative** and never absolute, which is the single trick that lets one string resolve through `apiAssetUrl` in the browser and against `API_URL` in email. The accent's real content is the **contrast policy**: one shared WCAG 2.2 implementation in `shared-validators` (two would drift and the drifting one is what the user sees), judged against **pure white** rather than the `#f8fafc` foreground because white is the strictest comparison that actually occurs, refusing below **3:1** with its own `ACCENT_CONTRAST_TOO_LOW` (422, remedy named, ratio quoted) and only *warning* below 4.5:1 — a single 4.5:1 rule would reject most real brand palettes for text the accent never carries. Application is **two custom properties**, `--color-primary` and `--color-ring` on the shell root, because Tailwind 4 resolves them at the use site; the ring moves with the primary deliberately (a slate focus ring on a re-tinted button is the bug this feature would otherwise ship) and `--color-primary-foreground` is deliberately untouched, which is exactly what the 3:1 floor buys. `accentColor: null` is **reset** and deletes the stored key; `undefined` leaves it alone. **Custom CSS has no schema field, no API and no UI — absent, not disabled** (ADR 0014 names the three threats and the five prerequisites, CSP `style-src` from Part 74 among them). Known warts: the public GET is unreachable to **API-key** callers because the default-deny route guard refuses a route with no authorization spec; superseded objects lean on Part 45's orphan sweep with no retry; no Playwright journey and no API-side suite for the logo service; the accent re-tints primary surfaces only. `workspaces.domain` stays inert — Part 73 owns it, extending `lib/api/api-origin.ts`. All quality gates green after two review rounds — see the record. |
|   73 | Complete | 2026-08-25 | Custom-domain support — a workspace reachable at its own hostname, and the part where `workspaces.domain` finally stops being an inert string by **losing its API input field**. The claim moved to a new `workspace_domains` table (migration `0022_fresh_maddog`) for one decisive reason: **a claim must be storable while it is still UNPROVEN**, and an unproven hostname sitting in the column that routing consults is exactly the tenant takeover this part exists to prevent. `workspaces.domain` is now a READ-ONLY MIRROR written only by `DomainsService`, in the same transaction as the claim row and the audit row (ADR 0006), set on a successful verification and cleared on removal *or a failed re-verification* — so a reader of that column is always reading a proved name. `domain` was therefore deleted from `createWorkspaceSchema`/`updateWorkspaceSchema`, from the REST controller, and from both tRPC mutations rather than deprecated: two writers with different rules, one requiring no proof at all, is not a state to leave running. `hostname` is **globally unique in the database**, because two workspaces claiming one address is not a conflict to reconcile later — it is a race, and only the database can settle it; the loser gets a 409 whose message is deliberately identical to "you already have a claim", since naming the other workspace would leak that a foreign tenant holds it. **BOTH DNS records are required and each covers the other's gap**: the TXT at `_notted-verify.<host>` proves ownership but not delegation (a workspace could hold the unique row for a name resolving elsewhere), while the CNAME direction is chosen by the claimant and alone proves only that somebody aimed a name at us. The apex fallback (no CNAME is legal at a zone apex, RFC 1034 §3.6.2) compares **full address SETS, not any overlap** — every address the host resolves to must also be the target's, or a claimant could keep serving the name elsewhere while holding our row. The verification token is stored in **plaintext on purpose**: its whole job is to be published in DNS, hashing it would make it impossible to tell the admin what to publish, and it still never reaches the CSV-exportable `audit_logs`. `GET /api/v1/domains/resolve` is the second deliberately unauthenticated route (Part 72's logo GET was the first) and is safe because it discloses only the mapping the administrator's own public CNAME already announces — identifiers only, `verified` rows only, and one identical 404 for unknown/pending/failed/malformed so it is not an enumeration oracle; it accepts `?host=` **and** `?domain=` because Caddy's `on_demand_tls ask` sends the latter with no way to rename it. Host-header injection is closed at the EDGE rather than sanitised at each read site: `TrustedHostMiddleware` runs via `app.use` ahead of helmet, CORS and Better Auth, refusing with **421 `UNTRUSTED_HOST`** — the status meaning "right server, wrong authority", so a proxy re-resolves instead of caching a negative — and exempting only the health probes, which orchestrators dial with whatever `Host` they please. `VerifiedHostsService` lives in **`common/`, not `domains/`**, because `AuthService` is one of its callers and `DomainsModule` imports `AuthModule`, so the natural home would have forced a `forwardRef`; it answers static configured hosts synchronously with no I/O (which is what keeps CORS and CSRF behaving identically when the feature is off) and tenant hosts asynchronously behind a TTL cache, failing **closed without caching the refusal**. Better Auth's `trustedOrigins` became an async function that must return the static list too — supplying a function REPLACES the array, so returning only verified origins would silently un-trust `APP_URL`. `CUSTOM_DOMAINS_ENABLED` is off by default and makes the routes **404, not 403**: the capability does not exist here, and a 403 invites the caller to find someone who can. On the web, Next 16's new `proxy.ts` refuses an unresolved host, refuses `/workspaces/<other-id>/…`, and refuses the workspace switcher on a tenant host — a **coherence** boundary, not an authorization one, since every API route re-authorizes anyway. Known ceilings: sessions are per host (host-only `Lax` cookies) and OAuth/magic-link/passkey always finish on the primary host; certificates are Parts 79/80/82's job; the verified-host cache is per process at 60 s positive / 10 s negative, so a freshly verified host can take a minute to answer everywhere; verification is manual with no re-check sweep; one domain per workspace. All quality gates green after two review rounds — see the record. |
|   74 | Complete | 2026-08-25 | Security hardening and abuse controls — the part whose main finding was that **CSRF protection was a convention, not a boundary**: `assertTrustedMutationOrigin` was called by hand at ~97 sites, so a controller added tomorrow that forgot the call was forgeable and nothing failed. `CsrfOriginMiddleware` now mounts once on `/api/v1` (mutating method + session cookie + no API-key actor), reusing the Part 73 `isTrustedOriginSync` seam rather than re-deriving origin logic — **and the manual calls stay**, because the middleware is the safety net, not the replacement. Second finding: **every brute-force limit was keyed by IP**, which credential stuffing rotates for free. `AuthLockoutService` adds the identifier axis — a 60 s budget (`RATE_LIMIT_AUTH_PER_MINUTE`, default 5, a tier tighter than `sensitive` on its own disjoint `:auth` bucket) and a lockout (`AUTH_LOCKOUT_ATTEMPTS` 10 / `AUTH_LOCKOUT_SECONDS` 900) **in Redis**, because a lockout that only holds on the instance that saw the failures is not a lockout. It could not live in the Express middleware at all: Better Auth needs the raw stream, so the body is first parsed in `hooks.before`, which is where the budget and lock are checked **before any validation, session lookup or password hash**. The identifier is SHA-256 everywhere — keys and logs — and the refusal message is byte-identical for known and unknown addresses, or the lockout would be an **enumeration oracle**. Third finding, in Better Auth itself: its IP resolution **trusts inbound `x-forwarded-for` unconditionally** and with no proxy collapses every caller into one bucket, so it is pinned to a private `x-notted-client-ip` that `main.ts` overwrites from Express's `request.ip`. Only a **401** counts as a failure (403 unverified / 400 malformed would let a bad client lock its own user out), and the sign-in outcome hook had to be **composed into** the existing rotation hook because 1.6.24's `hooks.after` is one middleware, not an array. Explicit helmet (`default-src 'none'` — an API has no legitimate subresource), web CSP via `apps/web/security-headers.js`, extended log redaction, and on-demand `pnpm security:check` (Trivy **pinned**; a run that scanned nothing says so rather than reading as a pass). **No migration, no new dependency, no authorization change.** Deliverables `docs/security/threat-model.md` and `remediation-checklist.md` record seven exceptions with owners and part deadlines — E1 CSP `'unsafe-inline'` (Next 16 inline flight scripts, no nonce seam) → 82; E2 in-memory general limiter → 80; E3 the `GHSA-36xv-jgw5-4q75` suppression, a genuine semver false positive but **unbounded by version**, so a bump to `@nestjs/core` v11 would silently keep hiding a real advisory; E4 production image scanning → 79; E5 per-host sessions → 82; E6 `pdfjs-dist` GHSA-hq66 and E7 the transitive `brace-expansion`/`nanoid` advisories → next dependency-matrix refresh. Review round 1 found the audit gate itself was the biggest hole: **`pnpm audit --ignore` is not a filter** — in pnpm 10.34.5 it switches `audit` into write-the-ignore-list mode and always exits 0 — and `auditConfig.ignoreCves` matches CVE ids, not the GHSA ids that were listed there, so `security:deps` had been passing unconditionally while four high advisories stood. |
|   75 | Complete | 2026-08-26 | Completing the automated test pyramid — a part whose real finding was that **the pyramid was already built and was reporting success it had not earned**. Turbo 2 filters the environment strictly, and `turbo.json` declared only `DATABASE_URL` on `test`/`test:ci`, so roughly **thirty `describe.skipIf` suites skipped in CI while the run printed green**; the declared set is now derived by grepping `process.env.` across the API test tree (not guessed) and includes `REDIS_URL`, `APP_ORIGIN`, `MINIO_*`, `MEILISEARCH_*`, `MAILPIT_*`, `BETTER_AUTH_*`, `RATE_LIMIT_*`, `FEATURE_*` and the auth/export switches — with `test/integration-gates.test.ts` turning "thirty suites skipped" into **one red test** whenever CI has a database but not the rest of the stack. Second hole: role denial was asserted **only as a UI affordance** — a button hidden or disabled inside five mega-tests — so nothing proved the *server* refuses when the UI is bypassed. `e2e/role-denial.spec.ts` provisions owner/admin/editor/viewer plus a stranger and drives every assertion through `page.request`, and its load-bearing claim is the **403-vs-404 split** `docs/authorization.md` promises: same-tenant permission failure is `403`, cross-tenant or guessed identifier is `404`, and collapsing them is a tenant-existence oracle every layer below would still call "denied". Status codes are asserted **exactly** (a `429` must not masquerade as a denial), positive controls are included so a wall of 403s cannot be a broken session, the refused delete is proved not to have deleted, and — because a wrong path also returns `404`, a *passing* result for half the spec — every path is first checked against the OpenAPI document the running API serves. Third: ADR 0002's REST/tRPC contract reuse was a convention with no test. `test/trpc-rest-parity.contract.test.ts` proves it **statically by reference identity** — both transports import the same exported schema object, so `===` is the strongest and cheapest check — with no database, no HTTP and no application boot, the routers built directly with `{} as never` services. The tRPC internals were **probed before being relied on** (@trpc/server 11.18.0 keeps `_def.inputs`/`_def.output` on built procedures) and the test asserts they are still there, so an upgrade that erases them fails loudly rather than degrading into `undefined === undefined`. All 39 procedures are classified in a bidirectional inventory, and `task.list` is recorded as a **deliberate divergence** (`taskListInputSchema` widens the REST schema so a JSON caller can send the boolean `false` rather than the string) with its assertion **inverted**, so resolving the divergence fails the test instead of leaving a stale note. Fourth: coverage was one aggregate. The global 70 is **deliberately not raised** — inflating one number is the superficial move the part warns against — and per-path floors were added for `src/authorization/**`, `src/auth/**`, `src/tenant/**`, `src/common/idempotency/**`, the four places a missed branch is a security bug. Those floors are **65, an admitted placeholder, not a measurement**: the session ran no tests and invented no number, and the ratchet to the measured floor (rounded down to 5, from `coverage/coverage-summary.json`) is the record's top follow-up. `e2e/accounts.ts` is the first shared Playwright fixture module since `mailpit.ts`, and **zero existing specs were migrated** — their copies diverged for real reasons. Two known dead suites are named rather than quietly fixed: `dashboard-shell.spec.ts` (8 tests, needs env nothing sets, superseded by Part 76) and `hybrid-search.integration.test.ts` (gates on `MEILISEARCH_URL`, a name `compose.yaml` never sets). Implement-only session: **no gate was run** and the record says so; the reviewer owns lint/format/type-check/test/build, the coverage ratchet, and flipping the status. **Verified 2026-08-26 (remediation).** All gates green. Two lint errors and three type errors in this part's own files had to be fixed first — including `accounts.ts` calling `APIResponse.request()`, a method Playwright does not have. The coverage ratchet is now **measured, not a placeholder**: authorization 85, auth 75, tenant 95, idempotency 95 (global stays 70), each the lowest of that path's four metrics rounded down to 5. `src/auth/**` could not have met any honest floor before: it measured 49.21 branches because the dev `api` container set neither `AUTH_E2E` nor a reachable Mailpit URL, so both auth e2e suites skipped — Part 75 widened `turbo.json` but never closed the compose side. With that closed it measures 79.05 branches. `hybrid-search.integration.test.ts` was **permanently dead**, gating on a `MEILISEARCH_URL` nothing sets; documenting that instead of fixing it was the wrong call, and it now runs. `app.e2e.test.ts` was failing on an inherited `BETTER_AUTH_TRUSTED_ORIGINS` that Part 75's own change made inheritable; `storage-maintenance.integration.test.ts` was asserting a **pristine database** (its `expiredExports` sweep is driven by each row's own `object_expires_at`, which no retention window bounds — 132 rows and 66 objects on this host); and `advanced-auth.e2e.test.ts` was rate-limited the moment it became reachable, on a sensitive-tier counter Better Auth keeps in **Redis**, shared across every process on the stack. Container suite: **235 passed / 2 skipped / 0 failed**, up from 230/5/2. **Closed 2026-08-26 (residual-closure pass): the Verify clause is now met in both halves.** The `note-images.spec.ts` flake was not host contention — it was a **product race**. `NoteEditorSurface` resolves this writer's display name from the member directory and that name was a **dependency of `useNoteCollaboration`'s effect**, so resolving it destroyed the collaborative provider, put `mode` back to `"pending"` (which renders a skeleton *instead of* the editor), and re-handshook onto a fresh `Y.Doc` — discarding every keystroke not yet on the server and orphaning the caret and insertion controller `useImageUploads` had recorded. That is why the upload answered 201 in 46 ms and the image still never rendered, why the typed paragraph vanished too, and why a **different** member failed each run: the guarding comment claimed the re-handshake lands "before anyone has typed", which is a race, not an invariant. Proved deterministically by a new spec that holds the directory back and counts ProseMirror roots (**2 mounts before the fix, 1 after**); fixed at the root by publishing the name into awareness in place (`setLocalName`, binding `user` as a getter) and dropping it from the effect deps. **No assertion in `note-images.spec.ts` was touched.** A second, unrelated latent flake surfaced and was fixed with the pattern its own sibling test already carried: `search.spec.ts:195` pressed `Control+K` once before `TopBar`'s keydown listener exists (reproducible 3 for 3 alone). Three full baselines: 79/1, then **82 passed / 0 failed / 9 skipped** twice from a freshly reset database, 7.9 and 7.8 min. Still a WSL2 floor, not a VPS-class certification. One container coverage run in four reported an unidentified single failure that did not reproduce in three retries — recorded, not rounded off. |
|   76 | Complete | 2026-08-26 | Accessibility and cross-browser validation — an audit whose honest shape is that **most of the accessibility work was already done and none of it was asserted**, with exactly one real defect underneath. That defect: the global focus indicator was `outline-none ring-2 ring-ring`, and Tailwind's `ring-*` compiles to a **`box-shadow`, which Windows High Contrast Mode discards outright** — so in forced colours the application had **no visible focus indicator at all** (WCAG 2.2 SC 2.4.7), invisible to every other test because every other test runs in normal colours. Fixed with an `@media (forced-colors: active)` block giving `:focus-visible` a real `outline: 2px solid Highlight`, which survives forced colours where `box-shadow` does not — **with `!important`, which is load-bearing rather than lazy**: `focus-visible:outline-none` is written into 20+ component class strings and Tailwind compiles every one into `@layer utilities`, a *later cascade layer* than the `@layer base` block, so the first draft of the fix would have lost the cascade and painted nothing on a single button. `forced-color-adjust` was considered for the presence palette and the Part 72 accent and **deliberately used nowhere** — colour is not the sole carrier of meaning in either, and opting out of the user's contrast choice would strip legibility from the white-on-hue presence label rather than protect it. That CSS block is the **only production change in the part**; everything else is test code or documentation. `e2e/axe.ts` is a shared non-spec helper injecting `axe.min.js` from an exact-pinned `axe-core@4.12.1` (already in the store transitively via `eslint-plugin-jsx-a11y`), tagged `wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`/`wcag22aa` with **`best-practice` and every AAA tag deliberately excluded** — advice is not the conformance target and a gate full of advice stops being read. Known violations are subtracted from the **completed** run via `ACCEPTED_VIOLATIONS` and **printed every time**, never suppressed with axe's own `rules: { enabled: false }`, because **a disabled rule produces no evidence** and the day the markup changes nothing says so. `@axe-core/playwright` was deliberately not added: its advantage is frame traversal and this app renders no cross-origin iframes. `e2e/accessibility.spec.ts` scans six surfaces and adds the three checks axe cannot make — a `forced-colors: active` focus assertion, focus order with dialog focus restore, and a polite-live-region invariant that is deliberately **not** "one region per page" — sonner's `Toaster` mounts one on every page and `PageContainer` documents why the editor runs a second, so the spec asserts **no region nests inside another** (the defect that makes every announcement arrive twice, and the one that only appears once components are composed), pinning `toHaveCount(1)` only inside `main` on the tasks route where `TaskListView` states that guarantee itself. `e2e/cross-browser.spec.ts` is deliberately small and covers only where the engines actually diverge, every choice made to avoid a Chromium-only API: contenteditable verified by **polling the persisted note row through REST**, never the save indicator (a synced collaborative session leaves autosave unbound); a **synthetic `ClipboardEvent`/`DataTransfer` carrying `text/html`**, never `grantPermissions(["clipboard-read"])` (Chromium-only) or a real OS-clipboard copy (flaky headless in WebKit); print under `emulateMedia({ media: "print" })` against a `PRINT_HIDDEN_SELECTORS` module extracted out of `print-export.spec.ts` (importing it *from* a spec re-registers that spec's tests), never `page.pdf()` (Chromium-only); reflow at 390 px and 1440 px with 200% zoom at 1440 only; and WebAuthn asserted as **graceful degradation** (`addInitScript` deleting `window.PublicKeyCredential`) because Playwright's virtual authenticator is CDP-only. Firefox and webkit are run **with an explicit spec path, never the whole suite** — a full serial chromium run is already 7–13 minutes at the standing `workers: 1` limit — and needed **no setup or config change**: `playwright.config.ts` already declared all three projects, `dev-tooling.mjs` only *defaults* to chromium, and the Playwright image ships all three binaries. **Edge is covered by substitution, recorded rather than assumed**: Chromium-equivalent here, its `msedge` channel needs a host binary the image lacks, and its divergences (own PDF viewer, `mica`/`acrylic`, WebView2) touch nothing in this app. Verified-not-built: drag alternatives (Move up/down/to-column/to-position plus `KeyboardSensor` on all four `@dnd-kit` surfaces), error association in `form-controls.tsx`, `one-time-code` autofill, and Part 72's shared contrast arithmetic. `dashboard-shell.spec.ts` stays **dead and untouched** — it gates on env nothing sets and the seed writes no credential accounts, so its users cannot sign in by password; its 8 tests are superseded but deletion waits until the superseding specs pass once. (An earlier draft called its `"Skip to main content"` assertion stale; that was wrong and is corrected in the record — there are **two** skip links from two layouts, the root layout's `"Skip to main content"` and the shell's `"Skip to workspace navigation"`, and the new spec asserts both in order.) Implement-only session: **nothing was executed** — no gate, no test, no browser, no screen reader — and `pnpm install` is required first because `apps/web/package.json` gained `axe-core`. Most importantly, **the manual keyboard/screen-reader half of the Plan.md criterion is NOT met and cannot be met by an agent**: the record lists the exact NVDA+Firefox and VoiceOver+Safari steps (landmark order, skip link as first stop, single sequential `h1`, toolbar roving focus and pressed state, dialog name and trap, one polite announcement not many, announced drag alternatives), and says plainly that axe — which detects roughly a third to a half of WCAG issues — is a floor, not a conformance claim. **Verified 2026-08-26 (remediation) — and none of it had ever run.** `axe.ts` opened with `createRequire(import.meta.url)`, but Playwright transpiles these specs to CommonJS, so `import.meta` was a *syntax* error that took down collection of every importing spec: `No tests found`. With that fixed, the first working scan found **two genuine WCAG 2.2 AA failures**, both now fixed — `target-size` (SC 2.5.8) on the editor's task-list checkboxes, and `scrollable-region-focusable` (SC 2.1.1) on the task board's column strip, the second being the finding this part had predicted and then scoped out. `NoteBoard.tsx` carried the identical pattern and got the same fix. Three harness defects too: a strict-mode locator that matched "Manage board columns", a **vacuous** 200%-zoom assertion (measured: CSS `zoom` on the root element does nothing in Chromium — computed `1`, `clientWidth` unchanged — now a 720 px viewport, the honest CSS-pixel equivalent), and Gecko's refusal to deliver `clipboardData` on a synthesised `ClipboardEvent`, now a measured capability gate rather than a hidden gap. axe was widened to two named `best-practice` rules that are `serious` (`aria-treeitem-name`, `aria-dialog-name`), `incomplete` results are now reported instead of silently discarded, and a `null` impact gates. Results: accessibility **9/9**, cross-browser **5/5 chromium**, **4+1 skipped firefox**, **5/5 webkit**. The `dashboard-shell.spec.ts` "now superseded" claim was **wrong** — six assertions have no equivalent anywhere, they are enumerated in the record, and the file must not be deleted. **Stays In progress:** the manual screen-reader pass is half the Plan criterion and no agent can perform it. **Closed 2026-08-26 (second remediation pass).** The stale "nothing has been executed" preamble was sitting directly above a table of Pass results and is rewritten. **All six orphaned `dashboard-shell.spec.ts` assertions are ported into `accessibility.spec.ts` and now execute** — the breadcrumb landmark, the <768 px mobile navigation dialog with its focus restore, the 820 px viewport, the repository's only `role="menuitem"` assertion, notification read-state persistence with mark-all-read, and the notification 503 path — taking the spec from 9 tests to **15/15**. Porting them found a seventh defect on their first-ever execution: the breadcrumb locator was a strict-mode violation, because the workspace overview route contributes a second `nav` whose name *contains* "Breadcrumb". Two assertions were also strengthened rather than transcribed: mark-all-read now provisions **two** mention notifications (with one, marking it read already empties the badge and the control under test does nothing), and the cleared-badge check is `exact` (the substring form is satisfied by "Notifications, 1 unread" and could never fail). The full chromium baseline was re-run correctly — fresh `e2e:up`, before the benchmark. **State is Complete on the AUTOMATED scope only:** the manual NVDA/VoiceOver pass and the real Windows High Contrast Mode check are recorded as named residuals with exact procedures, because no agent can perform either — and no automated result in the record is offered as screen-reader evidence. **Residual-closure pass 2026-08-26:** RESIDUAL 1 and RESIDUAL 2 are re-read and **left standing** — a real NVDA/VoiceOver pass and real Windows High Contrast Mode in Edge and Firefox are not agentable, were not attempted, and are not simulated; no screen-reader behaviour is claimed anywhere from an axe result. Both procedures are complete enough for a human to run without re-deriving anything. One correction to make RESIDUAL 1 followable: its check 2 named only `DashboardShell.tsx`'s "Skip to workspace navigation" as "the shell's real skip link", which would send a checker looking for the wrong first stop — there are **two**, and `app/layout.tsx:56`'s "Skip to main content" is the first focus stop on every page. |
|   77 | Complete | 2026-08-26 | Performance and scale limits — a part whose honest shape is that **almost everything it asks you to add was already there**, so the diff is a harness, a budget file, a standard, and deliberately **zero optimizations**. Verified before writing anything: pagination is capped at 100 rows by `paginationQuerySchema` and re-asserted per list schema; payload size is capped by a strict `json({ limit })` in `main.ts` with Nest's own parser disabled; the note and auth schemas already carry 13 and 6 indexes; backpressure is four rate-limit tiers plus bounded Redis leases, per-socket realtime leases, BullMQ concurrency and mandatory idempotency keys; virtualization is **not needed** and `NoteTimeline.tsx` already records that deferral in a `ponytail:` comment. The one genuine gap — no server-side `getOrSet` cache — was **left open on purpose**, with a pre-committed rule naming the single place it would be justified (the authorization hop inside `api.search`) *if a measurement points there*. That is the part's spine: **an optimization requires a failing measurement first**, and adding one without it destroys the evidence that the thing was ever slow. `scripts/perf-budgets.json` holds every number with a justification each; `scripts/perf-bench.mjs` is a plain Node 22 harness (global `fetch`, **no autocannon/k6/Lighthouse**) that provisions its own tenant through Mailpit and generates the fixture through the **real API** — raw SQL would leave no Meilisearch documents and no version snapshots, and generating through HTTP makes generation itself the `api.notes.create` sample set. p95 is **nearest-rank, never interpolated**, over ≥20 samples; a 429 makes a run `VOID`, not `FAIL`. Three budgets stay **`null`** because they need a production web build that does not exist until Phase 15 (`web-e2e` runs `next dev`), and `bulk.upload` is **`SKIPPED`** on memory-capped hosts by choice. **The benchmark has not been run** — that is Part 77's actual verification clause, so the record stays In progress with an empty results table and the command sequence waiting for it. **Measured 2026-08-26 (remediation) — the harness's first execution, which is where two defects surfaced.** `seed` could not complete at all: the large-document fixture was sized against `maxTotalText` alone and violated `maxChildren` (380 paragraphs on one `doc` node, cap 200), and the corpus-settle poll could not be satisfied — the obvious "page 10 of 100" is structurally impossible because `MAX_CANDIDATES` caps the search API at 200 authorized results, so the poll now walks to the deepest page the API *can* fill. **Results (WSL2, 6 vCPU, 8.9 GB RAM, shared Docker daemon — a floor, not a certification):** `api.notes.create` p95 78 ms / 400, `notes.list` 17 / 200, `notes.navigation` 14 / 500, `notes.read` 16 / 300, `tasks.list` 16 / 250, `search` 20 / 300, `ws.propagation` 7 / 1000 (real Yjs, no fallback) — and **`job.export.wait` p95 69,242 ms against 30,000: FAIL**. Diagnosed, not optimized around: a single export submitted alone with an empty queue took **40.7 s**, so queue depth explains none of it, and the same image renders a bare A4 page in 4.65 s — which is the pre-committed rule's "suspect Puppeteer cold start before the queue", i.e. a **Phase 15 packaging** concern. The audit's **"virtualization not needed because every list is capped at 100 rows" was false**: the sidebar tree requests `limit=500` against a ceiling of 1000 and `NoteTree` renders all of it. It now has its own budget and its own measurement (14 ms p95), so virtualization stays out of scope **on evidence**. `web.firstLoadJs` is now **NOT MEASURED**, not deferred: Next 16.2.11 prints no First Load JS column and its Turbopack output has no manifest to derive one from. **Closed 2026-08-26 (second remediation pass).** The one failing budget, `job.export.wait`, is disposed of **without editing the budget** — the mirror of this part's own pre-committed rule that an optimization needs a failing measurement first. Its "Puppeteer cold start" diagnosis is **retracted on evidence**: `browser-pool.service.ts` already keeps one shared Chromium warm for 60 s of idle and the benchmark never idles that long, so at most 1 of the 20 samples paid a launch and a **warm** `min` of 41,289 ms cannot be launch cost; dispatch is 1000 ms against a 41 s floor. The ~36 s gap between a bare A4 render (4.65 s) and a full export job is **unattributed**, because the harness collects no per-stage timing — so the row now reads *measured, over budget, cause not established*, owned by Phase 15 with **instrumentation, not optimization**, as the first task there and a standing "do not add a warm pool, it already exists" note in `docs/standards/performance.md`. Note also that Part 77's Verify clause names **VPS-class hardware**, which does not exist before Phase 15 — so a green table here would not have certified anything either. Complete on this hardware's scope; `bulk.upload` stays SKIPPED and four budgets stay NOT MEASURED with named blockers, never numbers. **Closed 2026-08-26 (residual-closure pass) by doing the instrumentation and nothing else.** Per-stage timings went into `export.worker.service.ts` and `pdf-export.service.ts`, the scenario was re-run, and the ~36 s had no owner because **it was never in the export path**: `queueWaitMs` p50 **1,594 ms** against `handlerMs` p50 **126 ms** — ~93 % of the wall time is outbox pickup, the render is 110 ms (browser-context acquisition 64, `page.pdf` 34) and the upload is 6. The mechanism is named: `note.created`/`task.created` have **no registered consumer**, so the dispatcher's own "rollout safety gate" re-claims and re-defers all 3,001 seeded rows forever at `3001/30 s` ≈ **100 rows per second**, which is exactly its entire `batchSize/interval` capacity — a new export queues behind the whole backlog. Row moves **FAIL → PASS** (n=20, p95 **2,591 ms** against 30,000) with the budget untouched, no warm pool added, and no optimization made. **Two numbers retracted on measurement:** a warm single export is **1,103 ms**, not 40.7 s; and a cold Chromium launch costs ~0.5 s (`renderMs` 587 vs 81), so the 4.65 s "bare A4 render" is not an export baseline either. **New residual, deliberately not fixed:** unconsumed outbox intents are never reaped, so past ~3,000 rows the dispatcher is saturated and *every* job's pickup latency degrades without bound — a queue-design decision outside this part. |
|   78 | Complete | 2026-08-26 | Observability and operational diagnostics — the part whose honest shape is that **logging, redaction and readiness were already good, and the correlation chain was quietly broken in the middle**. `job_outbox.correlation_id` existed, was indexed, and was consumed all the way through the dispatcher, the worker log lines, `QueueJobContext` and the dead-letter record — but **22 services insert into `job_outbox` directly and exactly one set it**, so the header→job half of the chain had been dead since Part 18. The fix is one line in the schema — a `$defaultFn` on `correlationId` reading `getRequestContext()?.requestId`, falling back to an SQL `null`. `$defaultFn` runs JS-side and **only when the caller omits the key**, so the explicit producer still wins, all 22 are fixed at once, every future producer is covered by construction, and drizzle documents it as runtime-only — **it emits no DDL, so there is no migration** (`pnpm db:check` is the evidence). Metrics are `prom-client@15.1.3` on a **module-scope registry**, not an injected `MetricsService`: a Prometheus registry is process-global by nature and two of the ~9 recording sites (an Express middleware bound with `app.use`, a BullMQ `worker.on("error")`) are not inside the DI container at all, so injecting one to reach a global would have added six constructor parameters and six test doubles and changed nothing about the value written. HTTP metrics extend the **existing** `response.once("finish")` handler in `request-context.middleware.ts` rather than adding an interceptor — that middleware is `app.use`'d first and therefore sees tRPC, Better Auth and Bull Board, which an interceptor structurally cannot — and the in-flight gauge decrements on **`close`, not `finish`**, because an aborted request never finishes and a gauge that only counts up is worse than none. **The single most important property is failure isolation:** `Registry.metrics()` awaits `Promise.all` over every metric, so ONE rejecting `collect()` turns the whole scrape into a 500 and Prometheus records **no sample for any metric** — HTTP rate, error rate, event-loop lag — at the exact moment a dependency is down; every collector is therefore try/caught and keeps its previous value, with a dedicated test that fails three sources at once (one throwing a `TypeError`, not a rehearsed rejection). Caching is a bare `nextAt` timestamp per collector rather than a cache class, because a gauge already retains its value between scrapes — "skip the query and return" **is** the cache — and `nextAt` advances only on success. `ReadinessService` was extracted from `HealthController` verbatim so the dependency gauge rides the existing 1 s cache instead of **doubling** every SMTP connection, MinIO round trip and Meilisearch call every 15 s. Queue depth was genuinely unmeasured (`getJobCounts()` had zero callers) and is now reached through a new count-only `QUEUE_METRICS_SOURCE` token mirroring `queue-readiness.indicator.ts` — the runtime owner stays private, because a collector that could reach a `Queue` could also `drain` or `obliterate` one — plus an **outbox depth** gauge, which is the one queue failure no BullMQ metric and no health check can see: a stalled dispatcher leaves `pending` rows piling up while every lane reads empty and readiness reads green. The worker takes **one** `performance.now()` and spends it twice, on the job histogram and on a `durationMs` field both existing log lines had been missing. Two brief assumptions were wrong and are recorded: **`DATABASE_POOL` is not exported from `DatabaseModule`** (`@Global()` makes a module's *exports* global, not its providers), so rather than widen the public surface to hand a collector a live `Pool` that can `connect`/`query`/`end`, `DatabaseService` gained a three-line `poolStats()`; and the new controller had to be registered in `OPENAPI_ROUTES` **and** the builder's `PREFIX_EXCLUDED` mirror, or `openapi.contract.test.ts` would have failed on an undocumented route — `docs/openapi.json` was regenerated (15 added lines). Exposure is a `METRICS_TOKEN` bearer compared with `timingSafeEqual` over SHA-256 digests (constant-time **and** length-independent) answering **404, never 401**, and unset by default: `requireOperator` needs a Better Auth **cookie session** a scraper cannot have, and Part 65 API keys are workspace-scoped by construction, so a shared secret was the only option — which is exactly why **no metric may carry a tenant identifier**, the weaker boundary being the reason for the label rules rather than an excuse. `metricLabel()` and `httpRouteLabel()` bound every non-literal label, the latter with a **hard 200-distinct-route cap** because segment sanitising bounds the *shape* of a label but not the *number* of shapes — a scanner walking `/api/v1/aaa`, `/aab`, … mints a new series per request and every one passes the segment rules. The forbidden list (`workspaceId`, `userId`, every entity id, `requestId`, email, IP, raw path, raw error message, object key, realtime room name) is asserted by a test that fails if a **UUID appears anywhere in the scrape body**, and AI, storage and export metrics are deliberately platform-wide because per-workspace numbers already exist, authorized, at `GET /api/v1/ai/usage` and the quota surface. `/metrics` is deliberately **not** exempt from `TrustedHostMiddleware` — the health probes are, because orchestrators dial them with any `Host`, but a token-gated route keeps every boundary it can — so a scraper on a raw IP gets `421` when custom domains are on; documented in three places rather than engineered away. Export duration is measured **in process** with the outcome the worker already knew at all seven exits and threw away; **no `started_at` column**, because `created→completed` would measure queue wait too and the known stuck-`processing` rows would poison every table-derived percentile. `errorSite` on a 5xx log is the first stack frame reduced by regex to `file:line:col` — the `Error` message line is skipped outright, since it routinely quotes the note title that caused the failure, and the capture charset excludes slashes, spaces, quotes and parens so the field structurally cannot carry content. No Sentry, no OTel, no outbound reporting of any kind. **No monitoring container, and the opt-in Compose profile was explicitly rejected**: a stack that only runs when the thing it watches runs is useless during the outage it exists for, it is ~1 GB RSS on a host already carrying a Chromium, and a profile nobody enables is dead configuration that rots. `ops/` is inert text for an existing Prometheus/Grafana — 14 alert rules each with a `for:` and a `runbook_url`, 12 `promtool` test blocks, and one 15-panel dashboard. The rules carry a **documented authoring rule commented in place**: per-instance facts aggregate with `sum`, but global facts read from a shared store — queue depth, outbox, storage, dependency health — are reported *identically by every instance* and MUST use `max by()`, or the backlog alert fires at 1/N of its threshold and that threshold **moves** with the replica count; `alerts_test.yml` has a negative test that fails if it is switched to `sum`. `NottedStorageGrowth` ships uncalibrated on purpose, commented as a knob, because every deployment's normal differs. `compose.yaml` also gains `LOG_LEVEL` (never set before) and an `x-log-rotation` anchor: Docker's default `json-file` driver has **no size limit at all**, so a crash loop could fill the host disk and take down every container on the daemon, not just Notted's. Implement-only session: **no gate was run** — no lint, type-check, test, build, `db:check`, `promtool`, scrape or failure injection — and the record says so, listing the reviewer's sequence including the two `expect: 0` greps that are the literal evidence for Plan.md's "without revealing secrets" clause. **Verified 2026-08-26 (remediation).** This part **did not compile**: seven sites assigned `gauge.collect = …`, which `prom-client@15` declares only on the *configuration* interface and never on the `Gauge` class — so the runtime behaviour was provably correct while `pnpm type-check`, `pnpm build` and the dev `api` container all failed, and `pnpm infra:up:ports` exited 1 with `dependency api failed to start`. One `setCollect()` helper confines the cast. Worse, **the cardinality guard the file's header is about had never fired**: `httpRouteLabel` read `request.path`, and Express rewrites `req.url` for `app.use(prefix, …)` mounts and only restores it in a `next()` that a terminating sub-handler never calls — so the scrape carried `route="/notes.list"` and `route="/sign-up/email"`, one series per tRPC procedure and per Better Auth sub-path, exactly the unbounded cardinality the buckets exist to prevent. Reading `originalUrl` fixes it (`route="trpc"`, `route="auth"` confirmed live) and also un-truncates the structured HTTP log, which had been recording stripped paths since it was written. The distinct-route cap was also **poisonable**: any unmatched path could spend a slot, so a 404 scan could permanently force every later-registered route to `other`; only a matched route registers a label now. Re-observed live: `/metrics` 404/404/200, every collector firing, **zero** UUID-shaped strings and no secrets in the scrape body. |

## Cross-cutting records

Work that is not a numbered `Plan.md` part. Keep rows ordered by date.

| Record | Status | Completed | Summary |
| --- | -------- | ---------- | ------- |
| [Coverage remediation](coverage-remediation-2026-08-04.md) | Complete | 2026-08-04 | Made `pnpm test:ci` pass in all four workspaces: the `turbo.json` env declaration that passes `DATABASE_URL` through, serialized DB suites, fixed a leaking concurrency fixture, fixed the archived-notes list query, and covered `src/lib`, the auth service/controller, and the shared route builders. |
| [All-in-Docker development](all-in-docker-development-2026-08-04.md) | Complete | 2026-08-04 | Replaced the infrastructure-only Compose stack with a root `compose.yaml` that runs infrastructure and both applications, so `docker compose up` is the only setup command, and cut the published port surface from nine to three (3000, 3001, 8025). Verified from a clean state, including registration through Mailpit email verification to an authenticated session. |
| [Disposable end-to-end stack](disposable-e2e-stack-2026-08-07.md) | Complete | 2026-08-07 | Gave Playwright its own `e2e` Compose profile — separate database, MinIO buckets and Redis logical database — because the suite had been running against `notted_dev` and failing on its own accumulated rows. Removing that noise exposed a real 500 on `GET /workspaces/:id/projects/:id`: drizzle returns timestamps as raw strings and a bare `sql<Date>` aggregate carries no decoder, so a false type predicate let a string reach `.getTime()`. Fixed with a typed `maxTimestamp` helper, an audit of all 8 `sql<...>` sites, and a regression test that exercises real driver marshalling. Suite now 19 passed / 0 failed. |
